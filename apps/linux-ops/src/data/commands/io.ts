import type { CommandItem } from '../../types';

export const io: CommandItem[] = [
  {
    id: 'io-iostat',
    title: 'Extended disk I/O statistics',
    description: 'The core tool for latency, throughput and saturation per device.',
    command: 'iostat -xz 1 5',
    category: 'io',
    tags: ['io', 'iostat', 'latency', 'await', 'saturation', 'disk'],
    risk: 'safe',
    explanation:
      '-x adds extended stats, -z hides idle devices, and the trailing numbers are interval and count. Watch %util (near 100% means saturated), await (ms per request) and the queue size.',
    alternatives: ['sar -d 1 5', 'dstat -d'],
    nextSteps: ['io-iotop', 'io-pidstat'],
  },
  {
    id: 'io-iotop',
    title: 'See which processes are doing the I/O',
    description: 'A top-like view of disk reads and writes per process.',
    command: 'sudo iotop -oPa',
    category: 'io',
    tags: ['io', 'iotop', 'process', 'disk', 'write'],
    risk: 'safe',
    requiresSudo: true,
    explanation:
      '-o shows only processes doing I/O, -P shows processes instead of threads, and -a accumulates totals instead of a live rate. Install the iotop package if it is missing.',
    alternatives: ['sudo pidstat -d 1', 'sudo iotop -b -n1'],
    nextSteps: ['io-pidstat'],
  },
  {
    id: 'io-lsof-deleted',
    title: 'Find processes writing to deleted files',
    description: 'A deleted-but-open file keeps consuming disk and can keep I/O busy.',
    command: 'sudo lsof +L1',
    category: 'io',
    tags: ['io', 'lsof', 'deleted', 'open', 'space'],
    risk: 'safe',
    requiresSudo: true,
    explanation:
      'Same as the disk-space version, but here the point is that the process is still writing to a file no one can see. Restarting or reloading that process releases both the space and the I/O.',
    alternatives: ['sudo lsof -nP | grep deleted'],
    nextSteps: ['disk-deleted-open', 'sd-restart'],
  },
  {
    id: 'io-dstat',
    title: 'Combined CPU, disk and network view',
    description: 'One stream that shows whether the bottleneck is CPU, disk or network.',
    command: 'dstat -d',
    category: 'io',
    tags: ['io', 'dstat', 'overview', 'cpu', 'network'],
    risk: 'safe',
    explanation:
      '-d focuses on disk. Running dstat with no flags gives a full dashboard. It is a fast way to decide which of the three subsystems to dig into.',
    alternatives: ['vmstat 1 5', 'iostat -xz 1 5'],
    nextSteps: ['io-iostat'],
  },
  {
    id: 'io-pidstat',
    title: 'Per-process I/O over time',
    description: 'Attribute disk activity to a PID instead of guessing.',
    command: 'pidstat -d 1 5',
    category: 'io',
    tags: ['io', 'pidstat', 'process', 'disk', 'rate'],
    risk: 'safe',
    explanation:
      'Prints read and write rates per process once a second. Combine with iostat: iostat shows the device is busy, pidstat shows who is making it busy.',
    alternatives: ['sudo iotop -oPa'],
    nextSteps: ['io-iotop'],
  },
  {
    id: 'io-await',
    title: 'Check I/O latency against a threshold',
    description: 'Turn iostat output into a single pass/fail on await.',
    command: "iostat -x 1 3 | awk '$1 ~ /^(sd|nvme|vd|xvd)/ {print $1, $10}'",
    category: 'io',
    tags: ['io', 'iostat', 'await', 'latency', 'threshold', 'awk'],
    risk: 'safe',
    explanation:
      'Column 10 is await in milliseconds. On SSDs it should stay in single digits; on spinning disks 10-20 ms is normal. A rising await with rising %util is the classic saturation signature.',
    alternatives: ['iostat -xz 1 3'],
    nextSteps: ['io-iotop'],
  },
  {
    id: 'io-readahead',
    title: 'Read the block device read-ahead',
    description: 'Check the kernel read-ahead size for a device.',
    command: 'sudo blockdev --getra /dev/{device}',
    category: 'io',
    tags: ['io', 'blockdev', 'readahead', 'device', 'tuning'],
    risk: 'safe',
    requiresSudo: true,
    explanation:
      'Prints the read-ahead in 512-byte sectors. Larger values help sequential reads and hurt random reads. It is a tuning knob, not a fix for a saturated device.',
    alternatives: ['cat /sys/block/{device}/queue/read_ahead_kb'],
  },
  {
    id: 'io-fio',
    title: 'Benchmark a disk with a known workload',
    description: 'Measure real throughput and latency instead of guessing from load.',
    command: 'sudo fio --name=test --rw=read --bs=1M --size=1G --filename=/tmp/fio.test --direct=1',
    category: 'io',
    tags: ['io', 'fio', 'benchmark', 'throughput', 'latency', 'change'],
    risk: 'change',
    requiresSudo: true,
    explanation:
      'Creates a temporary 1 GB file and reads it with direct I/O so the page cache does not hide the disk. Change --rw to randread for random access. It writes to /tmp, so clean up afterwards.',
    alternatives: ['dd if=/dev/zero of=/tmp/test bs=1M count=1024 oflag=direct'],
    nextSteps: ['io-iostat'],
  },
  {
    id: 'io-smart',
    title: 'Read the disk health report',
    description: 'Check for reallocated sectors, pending sectors and temperature.',
    command: 'sudo smartctl -a /dev/{device}',
    category: 'io',
    tags: ['io', 'smart', 'disk', 'health', 'failure', 'smartctl'],
    risk: 'safe',
    requiresSudo: true,
    explanation:
      'Look at SMART overall-health, Reallocated_Sector_Ct and Current_Pending_Sector. Any growth in those counters means the drive is failing, and slow I/O is often the first symptom.',
    alternatives: ['sudo smartctl -H /dev/{device}'],
    nextSteps: ['logs-dmesg', 'disk-remount-rw'],
  },
  {
    id: 'io-queue-depth',
    title: 'Check the device queue depth',
    description: 'See how many requests are in flight and whether the scheduler matters.',
    command: 'cat /sys/block/{device}/queue/nr_requests',
    category: 'io',
    tags: ['io', 'queue', 'depth', 'scheduler', 'device'],
    risk: 'safe',
    explanation:
      'nr_requests is the maximum number of queued requests. Also read /sys/block/{device}/queue/scheduler: on NVMe, none or mq-deadline is usually right, while the legacy cfq scheduler adds latency.',
    alternatives: ['cat /sys/block/{device}/queue/scheduler'],
    nextSteps: ['io-iostat'],
  },
];
