import type { CommandItem } from '../../types';

export const dns: CommandItem[] = [
  {
    id: 'dns-dig',
    title: 'Query a name',
    description: 'The full DNS answer: status, flags, TTL, records and the server that replied.',
    command: 'dig {host}',
    category: 'dns',
    tags: ['dns', 'dig', 'query', 'record', 'ttl', 'resolve'],
    risk: 'safe',
    explanation:
      'dig asks the resolver in /etc/resolv.conf by default. The ANSWER SECTION has the records, and the SERVER line names the resolver, which matters when different hosts resolve differently.',
    alternatives: ['dig {host} @1.1.1.1', 'nslookup {host}'],
    nextSteps: ['dns-trace', 'dns-ns'],
  },
  {
    id: 'dns-short',
    title: 'Get just the answer',
    description: 'A one-line answer for scripts and quick checks.',
    command: 'dig +short {host}',
    category: 'dns',
    tags: ['dns', 'dig', 'short', 'script', 'answer'],
    risk: 'safe',
    explanation:
      '+short prints only the record data, with no header. It is the form to use inside a shell script or a health check.',
    alternatives: ['host {host}', 'getent hosts {host}'],
    nextSteps: ['dns-dig'],
  },
  {
    id: 'dns-trace',
    title: 'Trace the delegation chain',
    description: 'Walk from the root servers down to the authoritative answer.',
    command: 'dig +trace {host}',
    category: 'dns',
    tags: ['dns', 'trace', 'delegation', 'authoritative', 'root'],
    risk: 'safe',
    explanation:
      'Asks the root, then the TLD, then the zone, showing where the delegation stops. If the trace succeeds but a normal query fails, the problem is your resolver or its cache, not the zone.',
    alternatives: ['dig +trace +additional {host}'],
    nextSteps: ['dns-ns', 'dns-resolv'],
  },
  {
    id: 'dns-reverse',
    title: 'Reverse lookup an IP',
    description: 'Find the PTR name behind an address.',
    command: 'dig -x {ip}',
    category: 'dns',
    tags: ['dns', 'reverse', 'ptr', 'ip', 'lookup'],
    risk: 'safe',
    explanation:
      '-x builds the in-addr.arpa query for the address. Mail servers and many logs rely on PTR records, and a missing or wrong PTR is a common cause of mail rejection.',
    alternatives: ['dig +short -x {ip}', 'host {ip}'],
    nextSteps: ['dns-dig'],
  },
  {
    id: 'dns-mx',
    title: 'List mail exchangers',
    description: 'See which servers accept mail for a domain, in priority order.',
    command: 'dig MX {host} +short',
    category: 'dns',
    tags: ['dns', 'mx', 'mail', 'smtp', 'priority'],
    risk: 'safe',
    explanation:
      'The number before each host is the preference: lower is tried first. MX records must point at hostnames, never at IP addresses, or mail delivery fails.',
    alternatives: ['dig +short MX {host}'],
    nextSteps: ['dns-dig'],
  },
  {
    id: 'dns-ns',
    title: 'List authoritative name servers',
    description: 'Confirm which servers are actually responsible for the zone.',
    command: 'dig NS {host} +short',
    category: 'dns',
    tags: ['dns', 'ns', 'nameserver', 'zone', 'authoritative'],
    risk: 'safe',
    explanation:
      'The NS records at the parent delegate the zone. Query one of these servers directly with @ to see the uncached answer and rule out resolver caching.',
    alternatives: ['dig +short NS {host}', 'dig {host} @ns1.example.com'],
    nextSteps: ['dns-trace'],
  },
  {
    id: 'dns-resolv',
    title: 'Show the configured resolver',
    description: 'See which DNS servers this host actually asks.',
    command: 'cat /etc/resolv.conf',
    category: 'dns',
    tags: ['dns', 'resolv', 'resolver', 'nameserver', 'config'],
    risk: 'safe',
    explanation:
      'The nameserver lines are queried in order. On systemd-resolved hosts this file points at 127.0.0.53 and the real configuration lives in resolvectl status.',
    alternatives: ['resolvectl status', 'systemd-resolve --status'],
    nextSteps: ['dns-systemd-resolve'],
  },
  {
    id: 'dns-systemd-resolve',
    title: 'Show systemd-resolved status',
    description: 'Per-link DNS servers, search domains and the current cache state.',
    command: 'resolvectl status',
    category: 'dns',
    tags: ['dns', 'systemd-resolved', 'resolvectl', 'link', 'status'],
    risk: 'safe',
    explanation:
      'Shows which DNS servers are used per interface. When a VPN is up, per-link DNS can send some names to a different resolver, which explains "it works from one host but not another".',
    alternatives: ['systemd-resolve --status'],
    nextSteps: ['dns-resolv'],
  },
  {
    id: 'dns-nslookup',
    title: 'Look up a name with nslookup',
    description: 'A portable alternative to dig, available on almost every system.',
    command: 'nslookup {host}',
    category: 'dns',
    tags: ['dns', 'nslookup', 'portable', 'query'],
    risk: 'safe',
    explanation:
      'Prints the server used and the answer. It is less detailed than dig but is present on Windows and minimal images, so it is handy for cross-platform checks.',
    alternatives: ['dig {host}', 'host {host}'],
    nextSteps: ['dns-dig'],
  },
  {
    id: 'dns-flush',
    title: 'Flush the DNS cache',
    description: 'Drop cached answers after a record change.',
    command: 'sudo resolvectl flush-caches',
    category: 'dns',
    tags: ['dns', 'flush', 'cache', 'systemd-resolved', 'change'],
    risk: 'change',
    requiresSudo: true,
    explanation:
      'Clears the systemd-resolved cache. It does not clear upstream caches, so if a change still does not appear, lower the TTL and wait, or query the authoritative server directly.',
    alternatives: ['sudo systemd-resolve --flush-caches'],
    nextSteps: ['dns-trace'],
  },
];
