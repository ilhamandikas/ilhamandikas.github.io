---
title: "Tools"
description: "A growing set of browser-based utilities for building, debugging and running software: formatters, encoders, crypto helpers, network and API testers, DevOps tooling and more. Everything runs in your browser — nothing is uploaded."
# The catalog page also emits the machine-readable registry, the search index and
# a Markdown version; every tool page emits a Markdown version of its own.
outputs: ["HTML", "ToolsJSON", "SearchIndex", "Markdown"]
cascade:
  outputs: ["HTML", "Markdown"]
---
