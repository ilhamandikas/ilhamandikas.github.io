# Long-form content for tool pages, keyed by slug.
#
# This is the part of a tool page a search engine has something to read. A tool
# page is otherwise just a form and some labels, which gives a crawler almost
# nothing to match a query against.
#
# Only tools listed here get an "About" + "Questions" section. There is no
# generated fallback on purpose: padding every page with near-identical filler
# would be worse than leaving it out.
#
# Fields:
#   about: one honest paragraph — what it does, how, and what it will not do
#   faq:   questions people actually ask, answered properly

qr-code-generator:
  about: >-
    This builds a QR code in your browser from any text or URL, with a choice of
    error-correction level and an adjustable margin. The code is produced locally
    by a bundled encoder, so the text you type is never sent anywhere. You can
    take the result as a PNG for a chat message or as an SVG, which is drawn as
    vector paths and therefore stays sharp at any print size.
  faq:
    - q: "Do I need an account, or is there a limit?"
      a: "No account, no API key and no quota. The generator runs entirely in the page."
    - q: "Which error-correction level should I choose?"
      a: "H is the most robust and survives roughly 30% of the pattern being damaged, at the cost of a denser code. Use L for short URLs on a screen, and H for anything printed on a sticker, a label or packaging that might get scuffed."
    - q: "Why did the PNG download fail?"
      a: "Drawing to a canvas can be blocked in some environments. The SVG download is pure vector, works everywhere and scales better for print anyway."
    - q: "How large can a logo be before the code stops scanning?"
      a: "Smaller than the error-correction percentage suggests, because correction works per block rather than across the whole symbol — a centred logo destroys one or two blocks outright while the overall average still looks healthy. Measured with an independent decoder over three payload lengths, the largest logo that still scanned was 12% of the code's width at L, 16% at M, 18% at Q and 24% at H, with the default white box behind it taken into account. Those numbers come from testing, not from arithmetic, and the page's hint turns red once you pass the limit for the level you chose."

wifi-qr-generator:
  about: >-
    Enter a network name, password and security type and this builds the standard
    WIFI: payload that Android and iOS understand, then renders it as a QR code.
    Scanning it joins the network without anyone typing a password, which is what
    makes it useful for guests, an office wall or a rental. The credentials stay
    in the page and are never put in the URL.
  faq:
    - q: "Does it work on an iPhone?"
      a: "Yes. The Camera app on iOS 11 and later reads Wi-Fi QR codes directly, with no extra app installed."
    - q: "Is it safe to print the password in a QR code?"
      a: "Treat a printed code exactly like the password itself — anyone who can photograph it can join the network. If you hand one out and later change your mind, change the password."
    - q: "Which security type should I pick?"
      a: "WPA/WPA2 for essentially every modern network, including WPA3 routers that still accept WPA2 clients. Use WEP only for genuinely old hardware, and 'no password' only for an open network."

json-formatter:
  about: >-
    Paste JSON and this validates it, pretty-prints it at the indentation you
    choose, or minifies it back down. The optional sort control reorders object
    keys at every level, ascending or descending, while leaving array order
    alone — which is exactly what you want when two API responses differ only in
    key order. Parsing uses the browser's own JSON parser, so it fails on exactly
    what a real parser fails on.
  faq:
    - q: "Does sorting also reorder arrays?"
      a: "No, and that is deliberate. Only object keys are reordered. Array order is data, so changing it would change the meaning of the document."
    - q: "What is the difference between this and the JSON Viewer?"
      a: "This one gives you text you can copy and paste. The viewer renders a collapsible tree, which is better for finding your way around a document whose shape you do not know yet."
    - q: "Is my data uploaded anywhere?"
      a: "No. Parsing happens locally with JSON.parse and nothing leaves the browser."

json-viewer:
  about: >-
    Two reads of one JSON document. The tree view collapses every branch, with a
    count of the nested values inside it and buttons to expand or collapse
    everything at once. The table view lays a list of records out as a grid, one
    row per record and one column per key, which is usually the fastest way to
    scan an API response. Both views are built for finding your way around a
    payload you have not seen before — an API response, a config file, a webhook
    body. The document is parsed locally.
  faq:
    - q: "What decides the columns in the table view?"
      a: "Every key that appears anywhere in the records becomes a column, in the order the keys first appear, with a row number in front when the document is an array. A key that only some records carry leaves an empty cell rather than shifting the row."
    - q: "How are nested objects and arrays shown in a cell?"
      a: "As compact JSON, cut off after about 120 characters. The full text is kept in the cell's tooltip. Use the tree view when you need to read a nested value properly."
    - q: "Can it open a large file?"
      a: "It handles documents in the megabytes. The browser still has to lay out every visible node, so very large files are easier to explore with the deep branches left collapsed."
    - q: "Can I get a single value out?"
      a: "Yes. The tree is there for reading, and the JSON Formatter is the tool for copying the whole document back out as text."

json-diff:
  about: >-
    Compare two JSON documents and see what was added, removed or changed, key by
    key. Because both sides are parsed first, differences in whitespace and key
    order are ignored and only real changes are reported — which makes it far
    more useful than eyeballing two formatted blobs.
  faq:
    - q: "Does key order count as a difference?"
      a: "No. Both documents are parsed and compared as structures, so reordering keys is not a change."
    - q: "Can it merge the two documents?"
      a: "No. It reports differences. Merging is a separate decision that depends on which side should win, and that is a judgement the tool cannot make for you."

base64-string-converter:
  about: >-
    Encode UTF-8 text to Base64, or decode it back, with an optional URL-safe
    alphabet where + and / become - and _ and the padding is dropped. Multi-byte
    characters are handled correctly because the text is encoded to UTF-8 bytes
    first, so accented letters and emoji round-trip without corruption.
  faq:
    - q: "What is URL-safe Base64 for?"
      a: "Standard Base64 uses + and /, both of which have to be escaped inside a URL. The URL-safe variant swaps them for - and _ so the value can sit in a query string or a filename untouched."
    - q: "Is Base64 a form of encryption?"
      a: "No. It is a reversible encoding with no key, and anyone can decode it. Use the AES tool if you need actual confidentiality."

base64-file-converter:
  about: >-
    Turn any file into a Base64 string, or a Base64 string back into a file. It
    is most useful for embedding a small image or font directly in a stylesheet
    as a data URI, or for working out what a data URI you were handed actually
    contains. The file is read locally through the File API.
  faq:
    - q: "Is there a size limit?"
      a: "Whatever your browser can hold in memory. Base64 inflates a file by about a third, so a 3 MB image becomes roughly 4 MB of text."
    - q: "Should I embed images as data URIs?"
      a: "Only for small files. A data URI cannot be cached separately from the stylesheet it lives in and it has to be parsed along with it, so anything past a few kilobytes is usually better served as an ordinary file."

hash-text:
  about: >-
    Compute MD5, SHA-1, SHA-256 and SHA-512 digests of any text. SHA-256 and
    SHA-512 use the browser's WebCrypto implementation; MD5 and SHA-1 are here
    because they still turn up in checksums and older protocols, not because
    they are safe for anything that matters.
  faq:
    - q: "Can I use MD5 for passwords?"
      a: "No. MD5 and SHA-1 are broken for anything security-related — both have practical collision attacks, and neither is designed to be slow. Use bcrypt or Argon2 for passwords."
    - q: "Why is there no SHA-3?"
      a: "WebCrypto does not expose it. Adding it would mean shipping a JavaScript implementation, which would be slower than native code and easy to get subtly wrong."

uuid-generator:
  about: >-
    Generate version 4 UUIDs, one at a time or in bulk, using the browser's
    cryptographic random number generator. They are suitable for database keys,
    request IDs and anything else that has to be unique without coordinating with
    a central authority.
  faq:
    - q: "Can two generated UUIDs collide?"
      a: "A v4 UUID carries 122 random bits. You would need to generate around 2.7×10^18 of them before a collision became likely, which is far beyond any realistic workload."
    - q: "Are these UUIDs predictable?"
      a: "No. They come from crypto.getRandomValues rather than Math.random, so earlier values tell you nothing about later ones."

ulid-generator:
  about: >-
    ULIDs are 128-bit identifiers like UUIDs, except the first 48 bits are a
    millisecond timestamp. That makes them sortable by creation time while
    staying unique, which is handy as a primary key when you want insertion order
    without keeping a separate timestamp column.
  faq:
    - q: "Should I use a ULID or a UUID?"
      a: "ULIDs if you want keys that sort by creation time and stay roughly chronological. UUIDs if you need the widest possible support, or must not reveal when a record was created."
    - q: "Are ULIDs case-sensitive?"
      a: "The canonical form is uppercase Crockford Base32, and that is what this tool produces."

token-generator:
  about: >-
    Generate random tokens and passwords of a chosen length from a chosen
    character set, drawn from the browser's cryptographic random source. Useful
    for API keys, session secrets and share links.
  faq:
    - q: "How long should a token be?"
      a: "For a machine-generated secret, 32 characters from a mixed alphabet is about 190 bits of entropy, which is far past brute force. Length buys you more than character variety does."
    - q: "Is Math.random good enough for this?"
      a: "No. Math.random is a fast seeded generator whose output can be predicted from previous values. This tool uses crypto.getRandomValues instead."

password-strength:
  about: >-
    Estimate how much work a password represents by measuring its entropy,
    assuming a specific number of guesses per second, and reporting how long a
    cracking rig would need. It is an estimate with stated assumptions rather
    than a verdict, which is the honest way to present this.
  faq:
    - q: "Why does my password score badly?"
      a: "Usually because it is short, or built from a predictable pattern. Length adds entropy far faster than swapping letters for symbols, so a long passphrase nearly always beats a short complicated string."
    - q: "Is my password sent anywhere?"
      a: "No. The estimate is computed in the page and nothing is transmitted."

jwt-parser:
  about: >-
    Decode, live-edit and verify JSON Web Tokens. Paste an existing token to
    inspect it, click the header or payload to edit the JSON and rebuild the token
    live, then check whether the signature still matches. Signature checking covers
    HMAC (HS256/384/512), RSASSA-PKCS1_v1_5 (RS256/384/512), RSA-PSS
    (PS256/384/512) and ECDSA (ES256/384/512).
  faq:
    - q: "Does this verify the signature?"
      a: "Yes, in the Check the signature panel, for HS, RS, PS and ES algorithms. Paste the shared secret for HMAC tokens or the PEM public key for the rest, and it recomputes the signature over the header and payload exactly as they arrived. That is why editing the payload by even one character flips the answer to a mismatch."
    - q: "Does a valid signature mean the token is trustworthy?"
      a: "No. It only means the token was signed by whoever holds the key you pasted. It says nothing about whether that issuer is one you trust, and it does not enforce exp, nbf, aud or iss — those are separate checks your application still has to make."
    - q: "Why is alg: none refused?"
      a: "Because alg: none means the token is unsigned, so anyone who can edit it can also rewrite the payload. A verifier that accepts the algorithm named in the token is the classic algorithm-confusion bug; here the algorithm is read from the header so the right primitive is chosen, and none is always reported as a failure."
    - q: "Is it safe to paste a token here?"
      a: "The decoding and the signature check both happen in your browser and nothing is uploaded. Even so, a token is a live credential — use a throwaway one when you are testing, and never paste a production secret."

timestamp-converter:
  about: >-
    Convert between Unix timestamps, ISO 8601 strings and local time, in both
    directions. It handles seconds and milliseconds, which is the mix-up that
    produces dates in 1970 or in the year 55000.
  faq:
    - q: "Is my timestamp in seconds or milliseconds?"
      a: "A ten-digit number is seconds; a thirteen-digit number is milliseconds. JavaScript's Date.now() returns milliseconds, while most Unix tooling works in seconds."
    - q: "Why is my date off by a day?"
      a: "Almost always a timezone issue. The converter shows UTC and your local zone side by side so you can see which one you were reading."

crontab-generator:
  about: >-
    Build a cron expression from its five fields and read it back in plain
    English, so you can check a schedule before it quietly does the wrong thing
    at 3am. It also covers the special strings like @daily and @reboot.
  faq:
    - q: "Why does 0 0 * * * run at midnight UTC?"
      a: "Cron uses the server's timezone. On most servers that is UTC unless the crontab sets CRON_TZ, or the machine itself is configured otherwise."
    - q: "Does this schedule the job?"
      a: "No. It only builds and explains the expression. Nothing is scheduled and nothing runs."

regex-tester:
  about: >-
    Test a regular expression against sample text and see every match highlighted
    as you type, with the capture groups listed separately. It uses JavaScript's
    own regex engine, so what you see here is what you get in a script.
  faq:
    - q: "Why does my pattern behave differently in Python or grep?"
      a: "The dialects differ. JavaScript has no atomic groups or possessive quantifiers, and lookbehind support is comparatively recent. The tool shows which flags it is applying."
    - q: "Why does .* match more than I expected?"
      a: "Greedy quantifiers take as much as they can and then backtrack. Append ? to make it lazy — .*? stops at the first opportunity."

regex-cheatsheet:
  about: >-
    A searchable reference for regular-expression syntax: character classes,
    anchors, quantifiers, groups, lookarounds and the flags that change how
    matching behaves.
  faq:
    - q: "Which dialect does this describe?"
      a: "The syntax is common to most engines. Where JavaScript, PCRE and POSIX diverge, the entry says so explicitly."

color-converter:
  about: >-
    Convert a colour between HEX, RGB, RGBA, HSL, HSLA and CMYK, with a live
    preview. It accepts shorthand hex and alpha channels, and reports the nearest
    CSS colour name when there is one.
  faq:
    - q: "Why is the CMYK value approximate?"
      a: "CMYK describes ink on paper and depends on the printer profile. The value here is the standard naive conversion, which is a reasonable starting point but not something to send to a print house."
    - q: "Does HSL use degrees?"
      a: "Yes. Hue is 0–360 degrees, while saturation and lightness are percentages."

chmod-calculator:
  about: >-
    Convert between octal permissions and symbolic ones, toggling read, write and
    execute for owner, group and others. It shows the resulting chmod command
    and the rwxr-xr-x string side by side, so you can check one against the
    other before running anything.
  faq:
    - q: "What is the difference between 644 and 755?"
      a: "644 gives the owner read and write and everyone else read, which is the normal choice for files. 755 adds execute for everyone, which is what directories and scripts need."
    - q: "What does a leading digit mean?"
      a: "It carries the setuid, setgid and sticky bits — 1777 for a shared directory like /tmp, for example."

ip-lookup:
  about: >-
    Show the public IP address you are connecting from, or look up any address,
    along with its country, region, city, coordinates, timezone and ISP. This is
    one of the few tools on this site that talks to the internet: it uses free,
    key-less services and falls back to a second one if the first is unavailable.
  faq:
    - q: "Why does it show a different city than where I am?"
      a: "Geolocation is based on the address your ISP has registered, not on your physical position, so it usually lands on the nearest city where the provider has infrastructure."
    - q: "Which services does it use?"
      a: "ipwho.is first, then freeipapi.com. Both are free and need no API key, and the tool tells you which one answered."
    - q: "Does this site store my address?"
      a: "No. The request goes from your browser straight to the provider and the result is rendered here. Nothing is logged by this site."

dns-lookup:
  about: >-
    Resolve A, AAAA, CNAME, MX, TXT, NS, SOA, CAA and SRV records over
    DNS-over-HTTPS, so the query travels over an encrypted connection instead of
    plain UDP on port 53. It asks Cloudflare first, falls back to Google, and
    reports which resolver answered.
  faq:
    - q: "Why is there no ANY query?"
      a: "Both Cloudflare and Google refuse ANY over DoH and return NOTIMP. Querying each record type individually is what the resolvers actually support."
    - q: "Is this the same as dig?"
      a: "Same records, different transport. dig speaks DNS on port 53; this uses HTTPS, so it works from networks that block port 53 and cannot be tampered with in transit."
    - q: "Why do I get a different answer than my own resolver?"
      a: "You are asking a public resolver with its own cache. A different answer usually means a different cache state, or a CDN handing back the node nearest to the resolver rather than to you."

whois-lookup:
  about: >-
    Show the registrar, registration and expiry dates, status codes and
    nameservers for a domain. It reads the registry's own RDAP service,
    discovered from the IANA bootstrap directory, so there is no third-party
    proxy in the middle. When a registry publishes no RDAP service at all, the
    tool says so instead of guessing.
  faq:
    - q: "Why do some domains return nothing?"
      a: "A few registries publish no RDAP endpoint at all — .io, .co, .de and several other ccTLDs among them. Without one there is no machine-readable record to fetch."
    - q: "Why is the registrant hidden?"
      a: "Since GDPR, most registries redact personal details on private registrations. The tool tells you when the response has been redacted rather than showing blanks."
    - q: "What is the difference between RDAP and WHOIS?"
      a: "RDAP is the structured HTTPS replacement for the old port-43 WHOIS protocol. It returns JSON instead of free text, so the fields can be labelled reliably rather than pattern-matched."

ipv4-subnet-calculator:
  about: >-
    Given an address and a prefix length, work out the network address, broadcast
    address, netmask, wildcard mask and usable host range, along with the total
    and usable host counts.
  faq:
    - q: "Why is the usable host count two less than the total?"
      a: "The first address in a subnet is the network address and the last is the broadcast address, and neither can be assigned to a host. The exceptions are /31 and /32, which the tool handles separately."
    - q: "Can it plan a VLSM layout?"
      a: "Not directly. Use it to check one block at a time, or the range expander to see which CIDR blocks cover an arbitrary range exactly."

ipv4-address-converter:
  about: >-
    Convert an IPv4 address between dotted-decimal, a single 32-bit integer,
    hexadecimal, octal and binary, and show the four octets broken out.
  faq:
    - q: "Why does 127.0.0.1 become 2130706433?"
      a: "The four octets are the bytes of one 32-bit number: 127×256³ + 0×256² + 0×256 + 1. That is why some sites accept the decimal form as the same address."
    - q: "Why would I need the integer form?"
      a: "Firewalls, access rules and some APIs store addresses as integers because they are cheaper to compare and range-check than strings."

ipv4-range-expander:
  about: >-
    Given a start and end address, list the CIDR blocks that cover the range
    exactly, so you can turn an arbitrary range into something a firewall or a
    route table will accept.
  faq:
    - q: "Why does it return several blocks instead of one?"
      a: "CIDR blocks are always powers of two, so an arbitrary range almost never lands on one. The tool finds the smallest set of blocks that covers it without going over."

markdown-to-html:
  about: >-
    Render Markdown to HTML in the browser, with the generated markup shown as
    text so you can copy it straight into a template. It handles headings, lists,
    tables, code fences, links and inline formatting.
  faq:
    - q: "Is the output sanitised?"
      a: "The renderer follows Markdown rules and passes raw HTML through. If you are rendering Markdown written by strangers, sanitise the result before putting it on a page."
    - q: "Which flavour of Markdown is this?"
      a: "CommonMark with the usual table and strikethrough extensions — GitHub-flavoured in practice."

text-diff:
  about: >-
    Compare two blocks of text line by line and see exactly which lines were
    added, removed or left alone. Useful for checking what actually changed
    between two versions of a config, a query or a paragraph.
  faq:
    - q: "Does it compare word by word?"
      a: "It compares line by line, which is the right granularity for configs and code. A whitespace-only change still shows up as a change."
    - q: "Is there a version for structured data?"
      a: "Yes. The JSON Diff tool compares two documents structurally, so it ignores formatting and key order and reports only real differences."

slugify:
  about: >-
    Turn any text into a URL-safe slug: lowercased, accents folded to ASCII,
    punctuation dropped and words joined with a separator you choose. Non-Latin
    scripts are transliterated where that is possible.
  faq:
    - q: "Why should a slug be ASCII?"
      a: "Percent-encoded characters are hard to read, easy to mangle when copied and awkward in analytics. A plain ASCII slug survives every tool it passes through."
    - q: "Should I use dashes or underscores?"
      a: "Dashes. Search engines treat a hyphen as a word separator and an underscore as joining two words, so my-post reads as two words and my_post as one."

url-encoder:
  about: >-
    Percent-encode and decode URL components, either the whole string or one
    component at a time. The distinction matters more than it looks: encoding a
    full URL turns its own ? and & into data, which is usually not what you want.
  faq:
    - q: "Should I encode the whole URL or just a parameter?"
      a: "Almost always just the parameter. Use component mode when the value goes inside a query string, and whole-URL mode only when the URL itself is the data being passed along."

lorem-ipsum:
  about: >-
    Generate placeholder text as paragraphs, sentences or words, starting from
    the classic Latin passage or a shuffled word pool. It is for filling a layout
    before the real copy exists.
  faq:
    - q: "Why not just write the real copy first?"
      a: "Because placeholder text makes it obvious what is a layout problem and what is a content problem, and it stops a review turning into an edit of the words instead of the design."
    - q: "Is the Latin real?"
      a: "It is a mangled excerpt of Cicero, which is the traditional joke. It is not meaningful Latin."

otp-generator:
  about: >-
    Generate time-based one-time passwords (TOTP, RFC 6238) from a shared secret,
    and verify a code you have been given. It shows the current code, the seconds
    until it rolls over and the next code, so you can compare against a device
    that may have drifted.
  faq:
    - q: "Is my secret sent anywhere?"
      a: "No. The HMAC is computed locally with WebCrypto. That said, do not paste a production secret into any page you do not control — including this one."
    - q: "Why does the code not match my authenticator app?"
      a: "Almost always clock drift. TOTP depends on both sides agreeing which 30-second window it is; the tool shows the remaining seconds so you can see the disagreement."

meta-tag-generator:
  about: >-
    Generate the meta tags a page needs for search and for social sharing: title,
    description, canonical, Open Graph and Twitter card tags, with a preview of
    how the result will look.
  faq:
    - q: "How long should a meta description be?"
      a: "Around 150–160 characters. Search engines truncate past that, and they will sometimes replace your description with text lifted from the page."
    - q: "Do Open Graph tags affect ranking?"
      a: "Not directly. They affect how a shared link looks, which affects whether people click it — and clicks are a signal."

http-status-codes:
  about: >-
    A searchable reference of HTTP response status codes, what each one means and
    when a server should return it.
  faq:
    - q: "When should I return 401 instead of 403?"
      a: "401 means the request is unauthenticated — the client should try again with credentials. 403 means it is authenticated but not allowed, and trying again will not help."
    - q: "Does 200 always mean success?"
      a: "For a GET, usually. For an API, a 200 carrying an error object is a common and unhelpful habit. Prefer a status code that matches the outcome."

svg-placeholder:
  about: >-
    Create a placeholder image as an SVG: pick the size, background and text
    colour and get a small vector file to drop into a layout while the real image
    is being produced.
  faq:
    - q: "Why SVG rather than a PNG?"
      a: "It is a few hundred bytes, stays sharp at any size and can be edited in a text editor. There is no reason to ship a bitmap for a solid rectangle."

roman-numeral:
  about: >-
    Convert between Roman numerals and decimal numbers, using the subtractive
    notation for 4, 9, 40, 90, 400 and 900. It covers 1 to 3999, which is the
    range the plain notation reaches without an overline.
  faq:
    - q: "Why does it stop at 3999?"
      a: "Standard Roman notation has no zero and no way to write larger numbers without an overline or a different convention. 3999 is MMMCMXCIX, the largest value the plain form expresses."

percentage-calculator:
  about: >-
    Work out percentages in the forms that actually come up: what X% of Y is,
    what percentage X is of Y, and the increase or decrease between two values.
  faq:
    - q: "Why do a percentage rise and fall not cancel out?"
      a: "Because they are relative to different bases. A 50% rise followed by a 50% fall leaves you at 75% of where you started, not back at the beginning."

temperature-converter:
  about: >-
    Convert between Celsius, Fahrenheit and Kelvin, with the formulas shown so
    you can check the arithmetic rather than trusting it.
  faq:
    - q: "Is there a temperature where Celsius and Fahrenheit agree?"
      a: "Yes, at −40 degrees. It is the single point where the two scales cross."

math-evaluator:
  about: >-
    Evaluate a mathematical expression with the usual operators, parentheses,
    functions and constants, and assign variables to reuse an intermediate
    result.
  faq:
    - q: "Is it safe to paste an expression in?"
      a: "Yes. The expression is parsed and evaluated as arithmetic. It is not handed to eval, so it cannot run arbitrary code."

docker-run-to-compose:
  about: >-
    Turn a docker run command into a docker-compose.yml service, mapping ports,
    volumes, environment variables, restart policy and the command into their
    Compose equivalents.
  faq:
    - q: "Does it understand every flag?"
      a: "It covers the common ones — ports, volumes, environment, restart, network, command and entrypoint. Anything it does not recognise is reported rather than silently dropped."

text-statistics:
  about: >-
    Count characters, words, sentences, paragraphs and lines, with and without
    spaces, plus an estimated reading time. Useful for checking a draft against a
    length limit.
  faq:
    - q: "How is the reading time worked out?"
      a: "Words divided by a fixed reading speed. It is a rough figure — technical material with code reads considerably slower than prose."

mime-types:
  about: >-
    Look up the MIME type for a file extension, or work backwards from a type to
    the extensions it covers.
  faq:
    - q: "Why does one extension have two types?"
      a: "Because of history. .js is text/javascript today, but application/javascript still appears in older configurations, and servers generally accept both."

mac-address-lookup:
  about: >-
    Look up the vendor registered to a MAC address's OUI — its first three
    octets — against the bundled IEEE registry.
  faq:
    - q: "Why does a lookup return nothing?"
      a: "Virtual machines and randomised addresses do not come from a registered OUI. Most phones now randomise their MAC per network, so a vendor lookup is often meaningless on modern hardware."

iban-validator:
  about: >-
    Validate an IBAN with the MOD-97 checksum, and read back the country, the
    check digits and the domestic account part.
  faq:
    - q: "Does a valid IBAN mean the account exists?"
      a: "No. The checksum only proves the string was not mistyped. It says nothing about whether the account is real or still open."

phone-parser:
  about: >-
    Parse and format a phone number by country, showing the international and
    national forms, the country calling code and whether the number is valid for
    that region.
  faq:
    - q: "Why does a number I know is real show as invalid?"
      a: "Validation checks the length and prefix rules for the selected region. A number can be genuinely in service and still fail those rules, for example if it was issued before a numbering plan change."

case-converter:
  about: >-
    Switch text between camelCase, PascalCase, snake_case, kebab-case,
    CONSTANT_CASE, Title Case and sentence case, with word boundaries inferred
    from the separators and capitalisation already present.
  faq:
    - q: "How does it split words that are already joined together?"
      a: "It splits on separators and on the case boundaries it can see, so getHTTPResponse becomes get, HTTP, Response. Where the input is ambiguous it can only ever be a guess."

ascii-text-drawer:
  about: >-
    Render text as large ASCII-art banners using a bundled FIGlet-compatible
    font, and copy the result as plain text. It is for README headers, code
    comments and terminal output.
  faq:
    - q: "Will the banner line up everywhere?"
      a: "Only in a monospaced font. In a proportional font the columns collapse, which is why the preview here is monospaced."

html-entities:
  about: >-
    Escape and unescape HTML special characters, choosing between named entities
    and numeric ones, so text can be embedded safely in a page or a snippet read
    back to see what it really contains. It escapes what needs escaping rather
    than every non-ASCII character, which keeps the output readable.
  faq:
    - q: "Which characters actually need escaping?"
      a: "In HTML text content, & and <. Inside an attribute value, also the quote character you are using to delimit it. Escaping more than that is harmless but makes the source noisy."

text-to-binary:
  about: >-
    Convert text to its UTF-8 bytes written in binary, and back again. Each
    character is shown as the eight bits that encode it, which is the clearest
    way to see how a string becomes bytes on the wire.
  faq:
    - q: "Why is one character sometimes more than eight bits?"
      a: "Because it is not ASCII. UTF-8 uses one byte for ASCII characters and up to four for others, so an accented letter or an emoji comes out as two or more groups of eight bits."

text-to-unicode:
  about: >-
    Show the Unicode code points behind a piece of text, as U+XXXX escapes and as
    decimal values, and turn those escapes back into text. It is the quickest way
    to find out what an invisible or look-alike character actually is.
  faq:
    - q: "What is the difference between a code point and a UTF-8 byte?"
      a: "A code point is the abstract character number. UTF-8 is one particular way of writing that number as bytes, so the same code point can occupy a different number of bytes in a different encoding."
    - q: "Why does text I copied have an invisible character in it?"
      a: "Usually a zero-width space or a non-breaking space picked up from a web page or a word processor. Listing the code points is what makes it visible."

text-to-nato-alphabet:
  about: >-
    Spell out text using the NATO phonetic alphabet — Alfa, Bravo, Charlie —
    which is what you use when a word has to survive a noisy phone line.
  faq:
    - q: "Why Alfa and Juliett, with a double t?"
      a: "Both spellings were chosen so that speakers of languages which do not distinguish those sounds still pronounce them correctly. The spelling is deliberate, not a typo."

string-obfuscator:
  about: >-
    Hide part of a string while leaving enough of it visible to recognise — the
    usual way to show a card number, an API key or an email address in a log, a
    ticket or a screenshot without exposing the whole value.
  faq:
    - q: "Is this a security measure?"
      a: "No. It is a presentation aid. The original value is still wherever you copied it from, and masking is not the same thing as redaction."

safelink-decoder:
  about: >-
    Extract the real destination from an Outlook SafeLink, which wraps the
    original URL in a redirect through Microsoft's safelinks service. It decodes
    the url parameter and shows where the link genuinely goes.
  faq:
    - q: "Why do SafeLinks exist?"
      a: "Microsoft rewrites links in email so it can scan the destination at click time and block it later if it turns malicious. That is why the address you see is not the real one."

basic-auth-generator:
  about: >-
    Build the Authorization: Basic header value from a username and password, and
    optionally produce a ready-to-use curl command. The encoding is plain Base64,
    which is exactly why it is only safe over HTTPS.
  faq:
    - q: "Is Basic authentication encrypted?"
      a: "No. The credentials are Base64-encoded, which anyone can reverse. They are protected only by the TLS connection carrying them, so never use it over plain HTTP."

json-minifier:
  about: >-
    Strip the whitespace out of a JSON document while keeping it valid, so it
    takes fewer bytes over the wire. It parses first and re-serialises, so a
    malformed document is rejected rather than mangled.
  faq:
    - q: "How much smaller does it get?"
      a: "Typically 20–40% for hand-formatted JSON. If the server gzips its responses the gain is mostly redundant, because gzip already removes repeated whitespace."

json-to-yaml:
  about: >-
    Convert JSON to YAML, preserving the structure rather than reformatting the
    text. It is the usual way to move a config between a tool that speaks one and
    a file that is written in the other.
  faq:
    - q: "Are comments kept?"
      a: "JSON has no comments, so converting JSON to YAML cannot invent any. Going the other way, YAML comments are dropped because JSON has nowhere to put them."
    - q: "Why does my document fail to convert?"
      a: "Usually an anchor or a custom tag with no equivalent, or a duplicate key. The error names the position."

yaml-to-json:
  about: >-
    Convert YAML to JSON, preserving nesting, arrays and types. Anchors and
    aliases are resolved, so the output is the effective document rather than the
    shorthand used to write it.
  faq:
    - q: "What happens to a non-string key?"
      a: "JSON requires string keys, so numeric and boolean keys are converted to their string form. That is a real difference between the two formats, not a bug."
    - q: "Are duplicate keys allowed?"
      a: "No. YAML permits them but almost every parser takes the last one silently, which hides mistakes. This tool reports them instead."

yaml-to-toml:
  about: >-
    Convert YAML to TOML, preserving nesting, arrays and types. TOML has no null,
    so a null value is reported rather than quietly dropped or turned into an
    empty string.
  faq:
    - q: "What happens to a null value?"
      a: "TOML has no null type, so it cannot be represented. The converter tells you which key it was, so you can decide what the absence should mean."
    - q: "Does key order survive?"
      a: "Yes, and it matters in TOML, because keys have to be defined before any table that uses them. The converter keeps the order it was given."

json-to-toml:
  about: >-
    Convert JSON to TOML, mapping objects to tables and arrays of tables to the
    TOML equivalent. Useful for turning an API response into a config file.
  faq:
    - q: "Why is a nested structure sometimes rejected?"
      a: "TOML requires that a table is fully defined before it is extended. A document that is legal JSON can therefore be inexpressible in TOML without restructuring, and the tool says so rather than emitting something invalid."

toml-to-json:
  about: >-
    Convert TOML to JSON, turning tables into nested objects and arrays of tables
    into arrays. Dates and datetimes are kept as strings in their TOML form.
  faq:
    - q: "How are TOML dates represented?"
      a: "JSON has no date type, so they become ISO 8601 strings — the same text, with the type information lost. That is a limitation of the target format."

toml-to-yaml:
  about: >-
    Convert TOML to YAML, turning tables into nested mappings and arrays of
    tables into sequences. Handy when a tool wants YAML but the config is written
    in TOML.
  faq:
    - q: "How are TOML dates represented?"
      a: "As ISO 8601 strings. YAML does have a timestamp type, but writing one that other parsers agree on is unreliable, so plain strings are the safer output."

xml-formatter:
  about: >-
    Pretty-print or minify XML at the indentation you choose. It parses the
    document first, so mismatched and unclosed tags are reported instead of being
    passed through.
  faq:
    - q: "Does it preserve CDATA and comments?"
      a: "Yes. Both are kept as they are; only the whitespace between elements changes."
    - q: "Why does it refuse my file?"
      a: "The parser is strict. A stray ampersand, an undeclared namespace prefix or a mismatched tag will stop it, and the error names the position."

xml-to-json:
  about: >-
    Convert XML to JSON. XML is a document format and JSON is a data format, so
    the mapping has to make decisions — attributes, text content and repeated
    elements all need a shape — and the tool states which convention it uses.
  faq:
    - q: "Is the conversion reversible?"
      a: "Not always, and no converter can promise that. XML can express things JSON cannot, such as mixed text and elements, or the same attribute twice. Check the output before relying on a round trip."

json-to-xml:
  about: >-
    Convert JSON to XML, with a root element you choose and a stated convention
    for arrays, attributes and primitive values.
  faq:
    - q: "What happens to an array?"
      a: "Each item becomes an element with the same name, which is the standard convention. The tool shows the rule it applied so the output is predictable."

json-to-csv:
  about: >-
    Flatten an array of JSON objects into CSV, using the union of the keys as the
    header row. It is the usual way to get an API response into a spreadsheet.
  faq:
    - q: "What happens to nested objects?"
      a: "They are flattened into dotted column names like user.address.city. An array inside a row is joined into a single cell, because CSV has no way to express nesting."
    - q: "Why are some cells empty?"
      a: "Because not every object has every key. The header is the union of all keys, so an object missing one simply leaves that cell blank."

sql-prettify:
  about: >-
    Reformat a SQL query with consistent indentation and uppercased keywords, so
    a one-line statement pasted out of a log becomes something you can read. It
    reformats; it does not validate.
  faq:
    - q: "Does it understand my dialect?"
      a: "It knows the common keywords and the usual quoting styles. It will not tell you whether the query is valid for your database, or whether it will do what you meant."

yaml-viewer:
  about: >-
    A collapsible tree view of a YAML document, showing the structure and the
    value counts on each branch. Anchors and aliases are resolved, so you see the
    effective document rather than the shorthand.
  faq:
    - q: "Are YAML anchors expanded?"
      a: "Yes, which means the tree shows what the document means rather than how it was written. That is usually what you want, but it does hide where the reuse came from."

list-converter:
  about: >-
    Turn a list into the shape you need: one item per line, comma-separated, a
    JSON array, a SQL IN clause or a numbered list. Useful for turning a column
    pasted out of a spreadsheet into something a query or a script will accept.
  faq:
    - q: "Does it deduplicate or sort?"
      a: "Only if you ask it to. By default the order and the duplicates are exactly as you pasted them."
    - q: "Why is my SQL IN clause broken?"
      a: "Almost always because an item contained a single quote. The tool escapes what it can, but read the output before running it."

hmac-generator:
  about: >-
    Compute an HMAC over a message using SHA-256, SHA-384 or SHA-512 and a shared
    secret, then copy the result as hex or base64. Paste an expected HMAC to
    check it against the message and key, which is how a webhook signature is
    normally verified. The computation uses WebCrypto.
  faq:
    - q: "What is the difference between an HMAC and a plain hash?"
      a: "A plain hash proves nothing about who produced it. An HMAC mixes in a secret, so only someone holding that secret can produce the same value — which is what lets you verify that a webhook came from the service you expect."
    - q: "Should I use hex or base64?"
      a: "Either works; they are the same bytes in different clothes. Providers differ: GitHub sends hex, Shopify sends base64, and many APIs use base64 because it is shorter. The verify box accepts whichever you paste, so you can compare across formats."
    - q: "How does the verify box decide if it matches?"
      a: "It computes the HMAC for the current message and key, then compares it with what you pasted. A value that is all hexadecimal is compared as hex, anything else as base64. Whitespace is ignored so a value split across lines still matches."

bcrypt:
  about: >-
    Hash a password with bcrypt, or verify a password against an existing bcrypt
    hash. The cost factor controls how much work each hash takes, and that
    expense is the whole point.
  faq:
    - q: "Which cost factor should I use?"
      a: "The highest one your server can afford at login time. The tool reports how long each hash took, which is the number that matters — a login should not take much more than about 250 ms."
    - q: "Why does the same password give a different hash every time?"
      a: "Because bcrypt generates a random salt per hash and stores it inside the result. That is what stops two users who chose the same password from having the same hash."

aes-encryption:
  about: >-
    Encrypt and decrypt text with AES-GCM using a passphrase, with the key
    derived by PBKDF2. The output is a self-contained Base64 blob carrying the
    salt and the initialisation vector, so it can be decrypted later with the
    passphrase alone.
  faq:
    - q: "Is this strong enough for real data?"
      a: "AES-GCM and PBKDF2 are sound primitives and the construction is standard. What it cannot protect you from is a weak passphrase, or forgetting it — there is no recovery path."
    - q: "Why does the ciphertext change every time?"
      a: "Because a fresh random salt and IV are generated for each encryption. The same plaintext and passphrase therefore produce a different blob, which is the intended behaviour."
    - q: "Is the passphrase sent anywhere?"
      a: "No. Deriving the key and encrypting both happen in the browser."

rsa-key-pair:
  about: >-
    Generate an RSA key pair in the browser with WebCrypto, in PEM form, ready to
    paste into an SSH or TLS configuration — and check whether a public key and a
    private key you already have are actually a pair. The private key never leaves
    the page unless you copy it out.
  faq:
    - q: "How many bits should the key be?"
      a: "2048 is the accepted minimum today. 4096 is slower to generate and to use for a modest margin, and 1024 should be treated as broken."
    - q: "How do you tell whether two keys match?"
      a: "WebCrypto cannot derive one key from the other, so the check signs a fixed throwaway message with the private key and asks the public key to verify it. If the signature verifies, the two halves share a modulus and are a genuine pair. The probe message is a constant, so the same two keys always give the same answer."
    - q: "Which key formats are accepted for the check?"
      a: "PKCS#8 and PKCS#1 for private keys, SPKI and PKCS#1 for public keys — that is BEGIN PRIVATE KEY, BEGIN RSA PRIVATE KEY, BEGIN PUBLIC KEY and BEGIN RSA PUBLIC KEY. The PKCS#1 forms are what openssl genrsa and openssl rsa -RSAPublicKey_out print, so they are wrapped into PKCS#8 or SPKI before WebCrypto sees them rather than rejected."
    - q: "Is a key generated in a browser trustworthy?"
      a: "The cryptography is the same WebCrypto implementation your browser uses for TLS, so the maths is sound. What you have to trust is the page itself, which is why generating keys offline is the safer habit for anything that matters."

bip39:
  about: >-
    Generate a BIP-39 mnemonic phrase from a chosen word count and derive the
    seed from it, which is how a cryptocurrency wallet backs up a key as words.
    It also checks a phrase you were given for a valid checksum.
  faq:
    - q: "Is it safe to generate a wallet seed in a browser?"
      a: "The entropy comes from crypto.getRandomValues, so the randomness is real. The risk is the page and the machine, not the maths. For anything holding real value, generate offline on a machine you control."
    - q: "Why does it reject my phrase?"
      a: "The last word carries a checksum. If one word was mistyped the checksum fails and the phrase is rejected, which is the feature working as intended."

pdf-signature-checker:
  about: >-
    Read a PDF and report the signatures it contains: who signed, when, and
    whether the byte range each signature covers still matches the file. It walks
    the PDF structure directly, with no upload and no external library.
  faq:
    - q: "Does this prove the signer's identity?"
      a: "No. It checks that the signature covers the current bytes and reports the certificate details. Whether to trust that certificate is a separate judgement, and this tool does not make it for you."
    - q: "Why is a signature reported as invalid?"
      a: "Usually because the file was changed after it was signed, which is the check doing its job. It can also happen with signature formats the reader does not recognise."

ipv6-ula-generator:
  about: >-
    Generate a random IPv6 unique local address prefix in fd00::/8, following
    RFC 4193, for a network that should never be routed on the public internet.
  faq:
    - q: "What is the difference between fc00::/7 and fd00::/8?"
      a: "The whole range is fc00::/7. The fd00::/8 half is where the 40-bit global ID is generated locally rather than assigned by a registry, which is the practical choice."

mac-address-generator:
  about: >-
    Generate random MAC addresses, optionally holding a prefix so the result
    stays inside a vendor range you specify.
  faq:
    - q: "Can I use a generated MAC on a real network?"
      a: "Technically yes, but if it collides with another device on the same segment both will break. Keep the locally-administered bit set so the address cannot clash with a registered vendor range."

random-port-generator:
  about: >-
    Pick one or more port numbers, with the option to stay above 1024 and to
    avoid the ports that are usually already taken.
  faq:
    - q: "Does it check whether the port is free?"
      a: "No. A browser cannot scan ports on another host. This picks numbers that are unlikely to clash — you still have to check your own machine."

url-parser:
  about: >-
    Break a URL into its parts — scheme, host, port, path, query parameters and
    fragment — and show each one both raw and decoded. It is the quickest way to
    see what a tracking-heavy link is really carrying.
  faq:
    - q: "Why is each parameter shown twice?"
      a: "Once percent-encoded exactly as it appears in the URL, and once decoded. The difference between the two is usually the interesting part."
    - q: "Why does a repeated parameter appear twice in the list?"
      a: "Because it is genuinely there twice. Repeated parameters are legal and often significant, so every occurrence is kept rather than the last one winning."

user-agent-parser:
  about: >-
    Break a user-agent string into browser, version, engine and operating system,
    and explain what each part is for.
  faq:
    - q: "Why is the browser detected wrongly?"
      a: "Because user-agent strings lie, deliberately. Every browser claims to be Mozilla, and some reduce or freeze the rest of the string. Treat any detection as a hint, and feature-detect instead whenever you can."

keycode-info:
  about: >-
    Press a key and see everything the browser reports about it: the key name,
    the legacy keyCode and whichCode, the physical position on the keyboard, and
    which modifier keys were held. It is for wiring up a shortcut without
    guessing.
  faq:
    - q: "Why is keyCode deprecated?"
      a: "It is inconsistent across layouts and languages — the same physical key reports different values. Use event.key for the character and event.code for the physical position, and press a key here to see the difference."

device-information:
  about: >-
    Show what the browser reports about the current device: screen and viewport
    size, pixel ratio, platform, language, timezone, CPU core count, memory where
    it is exposed, and whether the device claims to support touch.
  faq:
    - q: "Why is the reported platform sometimes wrong?"
      a: "User-agent strings are unreliable, and some browsers deliberately reduce them for privacy. Treat what you see as a hint rather than a fact."

camera-recorder:
  about: >-
    Record a short video from your webcam and download it. Nothing is uploaded —
    the recording is captured with MediaRecorder and held in memory until you
    save it.
  faq:
    - q: "Why can it not reach my camera?"
      a: "Camera access needs HTTPS and an explicit permission grant. If the button is disabled, the browser is missing MediaRecorder or reports no camera."
    - q: "Where does the recording go?"
      a: "Into memory in this tab, and then into your downloads folder when you save it. No part of it is sent anywhere."

html-wysiwyg-editor:
  about: >-
    A small rich-text editor that writes HTML, with bold, italic, headings, lists
    and links, and shows the markup it produced so you can copy it out.
  faq:
    - q: "Is the generated HTML clean?"
      a: "It is browser-generated, which is tidier than a word processor's output but still carries some inline styling. Expect to tidy it before using it in production."

git-cheatsheet:
  about: >-
    A searchable reference for the Git commands that come up less often: undoing
    a commit, recovering a deleted branch, rewriting history, and finding which
    commit introduced a line.
  faq:
    - q: "Is it safe to run these commands?"
      a: "Read what each one does first. The entries that rewrite history are marked, because on a shared branch they affect everyone who has pulled it."

emoji-picker:
  about: >-
    Search and copy emoji by name, with the whole set available rather than the
    subset your keyboard happens to offer.
  faq:
    - q: "Why do some emoji render as boxes?"
      a: "The font on your system does not include them. The character is still correct and will render wherever the font supports it."

numeronym:
  about: >-
    Turn a long word into a numeronym — the first letter, the number of letters
    omitted and the last letter, so internationalization becomes i18n.
  faq:
    - q: "Why is it called a numeronym?"
      a: "Because the omitted letters are replaced by their count. The pattern long predates the web — a11y for accessibility, k8s for Kubernetes, l10n for localization."

base-converter:
  about: >-
    Convert a number between binary, octal, decimal, hexadecimal and any base up
    to 36, working on the digits directly so long values are not rounded by
    floating point.
  faq:
    - q: "Why does the value not change when I paste a very long number?"
      a: "Because it should not. This converter works on the digits rather than through a floating-point number, so a 64-bit value keeps every digit — which is the whole reason to use it instead of a calculator."
    - q: "How are values above 9 written in base 36?"
      a: "With the letters A–Z after the digits, so 35 is Z and 36 is 10."

eta-calculator:
  about: >-
    Estimate a finish time from how much is done, how long it took and how much
    is left, assuming the current rate holds.
  faq:
    - q: "How accurate is it?"
      a: "Only as accurate as the assumption that the remaining work resembles the work already done. It is most useful for spotting an overrun early, not for planning."

chronometer:
  about: >-
    A stopwatch with lap times, counting in tenths of a second, for timing
    something without leaving the browser.
  faq:
    - q: "Does it keep running if I switch tabs?"
      a: "Yes. The elapsed time is computed from a clock reading rather than by counting ticks, so a backgrounded tab cannot make it drift."

benchmark-builder:
  about: >-
    Compare a set of measured values and see each one as a ratio against the
    fastest, so the differences are readable without doing the arithmetic.
  faq:
    - q: "Why ratios rather than percentages?"
      a: "Because 2.4× slower is easier to act on than 140% slower, and it does not break down when the baseline is the smallest value."

email-normalizer:
  about: >-
    Normalise an email address to a canonical form: lowercase the domain, strip a
    +tag from the local part, and remove dots for providers where they are
    ignored. Useful for deduplicating a mailing list.
  faq:
    - q: "Is removing dots always safe?"
      a: "No. Dots are only ignorable at specific providers — Gmail and a few others. At most providers first.last and firstlast are genuinely different mailboxes, so only apply this when you know the domain."

qr-editor:
  about: >-
    A QR code generator with the full set of style controls: module shape,
    finder-corner shape, a two-colour gradient, a transparent background, and a
    logo with its own size, padding, corner radius and background box. Six presets
    set the whole look at once if you would rather not tune each part. The encoder
    runs in the page, so the text and any logo you load stay on your machine. The
    hint under the Logo heading tracks the error-correction budget as you resize
    the logo and tells you when the code is likely to stop scanning.
  faq:
    - q: "How big can the logo be before the code stops scanning?"
      a: "That depends on the error-correction level, and the answer is smaller than the percentage suggests. Error correction works per block, not across the whole symbol, so a centred logo wipes out one or two blocks outright while the overall average still looks fine. Measured with an independent decoder, the largest logo that still scanned was 12% of the code's width at L, 16% at M, 18% at Q and 24% at H — with the default white box behind it, which erases modules of its own. The hint turns red past that number for the level you picked."
    - q: "Why is the round finder corner called a dot rather than a circle?"
      a: "Because the outer ring stays a rounded square. A fully circular outer ring broke detection outright in testing — the symbol stopped decoding with either a square or a round centre. A rounded square ring decoded with both. The dot style therefore rounds the ring and draws the inner 3x3 as a dot, which is the look people are after without the part that does not work."
    - q: "What is the difference between this and the plain QR Code Generator?"
      a: "They share the same encoder and both export PNG and SVG. This one adds the style controls — module and corner shapes, gradients, transparency and the logo panel with a live budget warning — and the presets. If you only need a plain black-and-white code, the simpler generator gets you there in one step."
    - q: "Can I use an SVG file as the logo?"
      a: "No, and the page says so rather than failing quietly. The logo is drawn into a canvas for the PNG export, and an SVG image loaded into a canvas taints it, which makes the export fail. PNG, JPEG, GIF, WebP and BMP all work."

jwt-editor:
  about: >-
    Build and sign a JSON Web Token in the browser. You edit the header and the
    payload as JSON, and the token line at the top is rebuilt as you type. Signing
    happens locally with WebCrypto, using a secret for HS256/384/512 or a PEM
    private key for RS, PS and ES algorithms — the key never leaves the page. The
    alg field in the header is rewritten to match the algorithm you choose, so the
    token cannot claim one algorithm while being signed with another.
  faq:
    - q: "Does this verify a token, or only create one?"
      a: "Only create. Verification — including the checks that a token claiming alg: none is refused and that an RSA key cannot be used to forge an HMAC — is what the JWT Parser tool does. Splitting them keeps each page honest about what it is doing: this one will happily sign whatever you give it, which is exactly what a forgery would look like, so it must never be mistaken for a check."
    - q: "Which key formats can I paste in?"
      a: "PEM. For RSA, both PKCS#8 (BEGIN PRIVATE KEY) and PKCS#1 (BEGIN RSA PRIVATE KEY) are accepted, because tools still emit both. For EC, PKCS#8 is required — an SEC1 EC PRIVATE KEY block is refused with an explanation rather than being half-parsed, because the curve is not named inside it and guessing it is how you sign with the wrong one."
    - q: "Why are the payload dates not shown as readable times?"
      a: "Because the payload is yours, not a schema. exp and iat are only numbers if you put them there, and an editor that rewrites your payload to show a date is an editor that changes what you are about to sign. The JWT Parser does annotate the registered claims, because reading a token that already exists is a different job."
    - q: "Is a token I sign here safe to use?"
      a: "Only for testing. The token is as good as the key you paste in, and this page has no idea whether that key belongs to your production system. Never paste a real signing key into a page you do not control, and treat any token built here as a throwaway."

http-request-tester:
  about: >-
    A request builder that also speaks curl. Paste a curl command and it is parsed
    into method, URL, headers and body; fill the form in and you can copy the
    equivalent curl command back out. The request itself is sent from your browser
    with fetch, straight to the address you type — nothing is relayed through this
    site, and no server of ours sees it. That also means the browser's own rules
    apply, which is the one thing this page cannot work around.
  faq:
    - q: "Why did a request fail with a CORS error instead of returning a response?"
      a: "Because a page cannot ignore the same-origin policy. The target has to allow cross-origin reads by answering with Access-Control-Allow-Origin, and if it does not, the browser blocks the reply before any script can read it. This is a rule of the browser, not a bug in the tool, and a static site has no server to proxy the request through — a proxy would also mean your headers, cookies and body passing through somebody else's machine. The page says this in the error rather than pretending the server is down."
    - q: "Which curl flags are supported?"
      a: "The ones that describe a request: -X, -H, -d, --data-raw, --data-binary, --data-urlencode, --json, -G, -I, -u, -b, -A, -e, -m, --url and the common clusters such as -sSL. Flags that ask for something a page cannot do — -k, -o, --http2, --cert, --limit-rate — are named in a \"Left out\" list rather than silently dropped, and flags that ask for something the browser already does, such as --compressed or -L, are reported as adjustments instead. The two lists are kept separate on purpose: mixing them teaches you to ignore the one that matters."
    - q: "Is the curl command I export the same as the one I pasted?"
      a: "For the parts that describe the request, yes — and where it differs, the page says so. Two examples it will tell you about: --data-binary sends no Content-Type at all in curl, while this form always sends one; and --data-urlencode encodes its value as a whole, where curl encodes only the part after the first =. Everything else, including -G moving data into the query string with the values percent-encoded, round-trips unchanged."
    - q: "Can I send cookies to another site?"
      a: "Only if the request is same-origin or the target explicitly allows credentials. Cross-origin fetch with credentials requires the server to answer with Access-Control-Allow-Credentials as well as a specific origin, and never a wildcard. The credentials dropdown lets you ask; the browser decides."

nginx-config-generator:
  about: >-
    Build an nginx server block by answering a few questions: which hostnames,
    static files or a reverse proxy, TLS or plain HTTP, caching, compression,
    security headers and logging. The output is a complete file you can drop into
    sites-available, and every line of a nested block is indented so it reads as
    the block it is. Anything the tool could not do properly is written into the
    output as a # !! comment rather than left in a status line, so a config that
    would not load cannot be mistaken for one that will.
  faq:
    - q: "Is the generated config checked against a real nginx?"
      a: "No, and that is worth being clear about. nginx is not installed on the machine this was built on, so the output is checked structurally — every server_name is validated against the hostname pattern, braces are balanced, indentation is consistent — and not with nginx -t. Read it before you reload, which is good practice with any generated config."
    - q: "Why is the www redirect written out as two server blocks?"
      a: "Because nginx has no variable that means \"this host without the leading www\". There is no way to derive one hostname from another inside a server block, so the only correct output is one redirect block per literal pair you typed. A single clever regex would silently fail for the domains it did not match."
    - q: "Why is http2 on; mentioned in the notes?"
      a: "Because that directive only exists from nginx 1.25.1 onwards; older versions used the http2 parameter on the listen line. The generated file uses the current form and the notes say which version it needs, so you find out before the reload fails rather than after."
    - q: "Why are the security headers repeated inside the cache location?"
      a: "Because add_header does not merge. A location block that sets any header of its own discards every add_header inherited from the parent, so the asset-cache block would quietly lose the security headers — on exactly the responses that are cached and served most often. Repeating them is the standard workaround, and the notes say why they are there twice."

image-metadata:
  about: >-
    Drop in a JPEG, PNG, GIF or WebP and read the metadata the file actually
    carries: camera make and model, exposure settings, orientation, dimensions,
    colour profile, PNG text chunks and, where present, GPS coordinates with a
    link that opens the location in Google Maps. The parsing happens in the
    page, so the picture is never uploaded. A raw JSON panel shows every tag that
    was read, which is what you want when a field looks wrong.
  faq:
    - q: "Why does my image show no EXIF at all?"
      a: "Because most images no longer have any. Messaging apps, social networks and screenshot tools strip metadata on purpose, and any image that has been through an editor and re-exported usually loses it too. \"Read, but there is no EXIF in this file\" is a normal result, not an error — it does not mean the file carries nothing."
    - q: "Is the GPS location exact?"
      a: "It is exactly what the file says, to as many decimal places as the file stores. Many cameras write a rounded position, and some write a position that was never fixed at all, so a coordinate in a file is a claim by the camera rather than a measurement you can rely on. The page shows the degrees, minutes and seconds it read alongside the decimal pair so you can check the conversion yourself."
    - q: "Can it remove metadata?"
      a: "No. It only reads. Stripping metadata means re-encoding the image, which is a lossy step (the Image Compressor does exactly this) — and it is worth knowing that the reliable way to remove EXIF is to re-export the picture from an editor, or to use a dedicated metadata stripper, rather than to trust a viewer."
    - q: "Which formats are supported, and why those four?"
      a: "JPEG, PNG, GIF and WebP — the four whose metadata can be read from the file structure alone without a decoder. TIFF shares EXIF's structure with JPEG and is read where it appears inside one of these, but a standalone .tif is not accepted, because supporting it properly means supporting every TIFF variant including the ones that are not images at all."

image-compressor:
  about: >-
    Pick a JPEG, PNG, GIF or WebP, cap its width and height, choose the output
    format and quality, then download the result — all in the page, so the file
    is never uploaded. The resize is a plain canvas re-encode: the dimensions
    are treated as ceilings, so a photo is only ever shrunk and a smaller image
    is left at its own size. Re-encoding also drops EXIF, GPS and camera
    settings, which is a useful side effect when you want to share a picture
    without the location it was taken.
  faq:
    - q: "Does compressing remove the metadata?"
      a: "Yes, for the file you download. The pixels are redrawn onto a canvas and written out again, and a canvas only carries pixels, so EXIF, GPS and camera settings are not part of it. That is convenient when you want to strip a location before sharing, but it also means a rotated photo can come out the wrong way round, because the orientation tag that told viewers how to display it is gone."
    - q: "Why did my translucent PNG turn black or white?"
      a: "Because the format you chose does not keep transparency. JPEG has no alpha channel, so transparent pixels are filled with the encoder's default colour, usually black. Keep PNG or WebP selected when the image has a transparent background, and switch to JPEG only when every pixel is opaque."
    - q: "The file got bigger instead of smaller — why?"
      a: "Re-encoding is not automatically a win. Re-encoding a photograph to PNG usually changes little, and turning a small, already-optimised JPEG into a PNG can easily double it. The quality slider only applies to JPEG and WebP; for those two, lowering it is what actually shrinks the file."
    - q: "Is there a size limit?"
      a: "Only what the browser and its memory can hold. Everything runs on this device, so a very large panorama can be slow or hit the canvas size limit, around 16,000 pixels a side in most browsers. If a huge image fails, cap the width or height and try again."

file-hash-checker:
  about: >-
    Compute MD5, SHA-1, SHA-256 and SHA-512 for a file in your browser, then check
    the result against a hash you were given. The file is read locally and never
    uploaded. Pasting a published digest — including a whole sha256sum line — turns
    the result into a verdict rather than a row of hex for you to compare by eye,
    and re-pasting a different digest re-compares without reading the file again.
  faq:
    - q: "Is MD5 still worth using?"
      a: "For checking that a download arrived intact, yes, and it is still what a lot of publishers list. For anything where someone might have a reason to give you a file that differs from the one you asked for, no — MD5 and SHA-1 are both broken for that purpose and collisions can be constructed deliberately. Use SHA-256 for anything security-related. The page offers all four because the digest you were given decides which one you need, not the other way round."
    - q: "Why is SHA-512 off by default?"
      a: "Because it is rarely the one published, and every algorithm you tick is another pass over the file. All four can be ticked at once if you want them; the checkboxes only control which rows are computed."
    - q: "What happens with a very large file?"
      a: "It is hashed in chunks, so memory does not grow with the file — but there is a 256 MB ceiling, and the page states it up front rather than failing halfway. Beyond that, use sha256sum on the command line, which is faster and has no such limit."
    - q: "I pasted a hash and it said the lengths are not comparable. What does that mean?"
      a: "That the digest you pasted is a different length from every algorithm currently ticked, so the two cannot be compared at all. Calling that a mismatch would be a false accusation against the file, so the page says which of the two situations you are in and tells you to tick the matching algorithm."

ssh-key-generator:
  about: >-
    Generate an SSH key pair in the browser — Ed25519, ECDSA on P-256, P-384 or
    P-521, or RSA at 2048, 3072 or 4096 bits — and download the private key in the
    format your own ssh reads. The private key is OpenSSH's native
    openssh-key-v1 container, which is what ssh-keygen writes and what ssh accepts,
    with a PKCS#8 PEM copy offered beside it for tools that want one. Keys are
    generated locally with WebCrypto and are never sent anywhere.
  faq:
    - q: "Why is the private key in OpenSSH format instead of PKCS#8?"
      a: "Because PKCS#8 Ed25519 is not readable by OpenSSH. Measured on OpenSSH 9.6, ssh-keygen -y on a PKCS#8 Ed25519 key fails with \"invalid format\", while the same key in the openssh-key-v1 container works. PKCS#8 is convenient and widely supported by other tools, so it is offered as a second, clearly labelled panel — but the file you download for ssh is the one ssh can actually open. Claiming the wrong one is portable would be worse than the extra panel."
    - q: "How do I use the key once I have downloaded it?"
      a: "Save the private key, then chmod 600 it — ssh refuses to read a private key that anyone else can open, and that refusal is a feature. Copy the public key into ~/.ssh/authorized_keys on the server, or hand it to whatever service you are setting up. The public key is the one-line ssh-ed25519 or ssh-rsa form that starts with the key type."
    - q: "Which key type should I pick?"
      a: "Ed25519 unless you have a reason not to: it is small, fast, and has no parameter choices to get wrong. Use RSA if you have to talk to something old — 3072 bits or more, since 2048 is the floor rather than a recommendation. ECDSA is there for compatibility with systems that expect it; it is fine, but there is no advantage over Ed25519 on a modern system."
    - q: "Are the fingerprints real, or does the page just make them up?"
      a: "They are computed from the same wire format ssh uses, and they were checked against ssh-keygen as an independent oracle — both the SHA-256 and the MD5 form, for every key type the page offers. The private key was checked further: a real ssh-keygen -Y sign and -Y verify round trip, plus openssl pkey -check on the PKCS#8 copy. That is stronger evidence than the page agreeing with itself, but it was measured on Linux OpenSSH 9.6 — older OpenSSH and Windows tools such as puttygen are untested here."

websocket-tester:
  about: >-
    Open a WebSocket from your browser, send text or binary frames, and watch
    everything that arrives in a timestamped log. The connection is made by the
    page itself, so it goes from your machine to the server you name and through
    nothing else. Binary frames can be typed as hex and are converted for you, and
    JSON that comes back is indented so it is readable. Close codes are explained
    rather than just printed.
  faq:
    - q: "Why can I not connect to a ws:// address from this page?"
      a: "Because the page is served over https, and a browser refuses to open an insecure WebSocket from a secure page — it would be a hole in the padlock. This is a mixed-content rule enforced by the browser, not a setting here. Use wss://, or run the test from a page served over plain http, or use a command-line client such as websocat for a local ws:// endpoint. The page allows ws:// to localhost, which browsers treat as trustworthy."
    - q: "What does close code 1006 mean?"
      a: "That the connection closed without a close frame — no proper handshake, no reason string. It is what you see when the server process died, a proxy dropped the connection, or the network went away, and it is deliberately not a code a server can send you. The log spells this out because 1006 on its own looks like a mystery number."
    - q: "Can I send binary data?"
      a: "Yes. Tick \"Send as binary\" and type the bytes as hex, and the page converts them to an ArrayBuffer before sending. Anything that is not valid hex is refused with a message rather than being sent as something else. Replies are labelled as text or binary in the log, and binary replies are shown as hex."
    - q: "How do I connect with a subprotocol?"
      a: "Type them in the subprotocol field, comma separated — for example chat, superchat. The server picks one and the log records which one it chose, which is the part people actually need to see when a handshake succeeds but the application protocol is wrong."


nik-parser:
  about: >-
    Parse an Indonesian NIK/KTP number locally in the browser. The tool splits the
    16 digits into province, regency or city, district, encoded birth date, gender
    marker and serial number, then looks up the region names from an offline copy
    of Indonesian administrative codes. It does not verify whether a NIK belongs
    to a real person; it only explains the structure of the number you typed.
  faq:
    - q: "Can this validate that a NIK is real?"
      a: "No. It can validate the format and decode the embedded fields, but only Dukcapil or an authorised system can confirm whether the identity exists and is active. Treat this as a parser, not an identity check."
    - q: "Why does a female birth date look larger than 31?"
      a: "In a NIK, the day of birth is stored directly for male holders and with 40 added for female holders. So 01 means male born on the 1st, while 41 means female born on the 1st. The page shows that explanation beside the decoded gender."

random-generator:
  about: >-
    Pick one or more random items from a list, with a choice of simple animations:
    spin wheel, slot machine, random card, dice roll or ticker. The draw uses the
    browser's crypto random source and runs entirely on the page, so your list is
    not uploaded. Turn off "No duplicates" if the same item is allowed to win more
    than once.
  faq:
    - q: "Is the animation deciding the result?"
      a: "No. The result is picked with crypto.getRandomValues, then the animation is only a reveal. That keeps the draw fair even if the animation frame rate changes."
    - q: "Can I pick multiple winners?"
      a: "Yes. Increase the Pick number. With No duplicates enabled, an item can appear only once and the count is capped by the number of items in the list."

curl-tester:
  about: >-
    Parse a curl command locally, show the HTTP method, URL, headers and body it
    contains, then rebuild a cleaner command. It is split from the HTTP Request
    Tester so command conversion does not clutter the actual request/response UI.
  faq:
    - q: "Does this send the request?"
      a: "No. It only parses and rebuilds curl. Use the HTTP Request Tester when you want the browser to send a request."

grpc-tester:
  about: >-
    Build grpcurl commands for native gRPC and test gRPC-Web endpoints from the
    browser. Native gRPC uses HTTP/2 features that browsers do not expose directly,
    so browser requests here are explicitly gRPC-Web requests.
  faq:
    - q: "Can a browser call native gRPC?"
      a: "Not directly. Browsers can use gRPC-Web through a compatible server or proxy. For native gRPC, use the generated grpcurl command from a terminal."

cors-checker:
  about: >-
    Check whether a URL can be read by JavaScript from this site origin. The tool
    sends a browser fetch request and, where possible, an OPTIONS-style probe, then
    reports what the browser allowed the page to see. It cannot spoof arbitrary
    Origin headers because browsers deliberately do not allow that.
  faq:
    - q: "Why does it say blocked without showing the server headers?"
      a: "When CORS blocks a response, the browser hides the response from JavaScript. That is the rule this tool is testing, so sometimes the only honest answer is that the page could not read the details."

sticker-maker:
  about: >-
    Make a 1024×1024 PNG sticker from an uploaded image, with circle, square or
    rounded frames, decorative text, a sticker outline and simple local background
    removal. The image is processed in the browser canvas and is not uploaded.
  faq:
    - q: "How good is the background removal?"
      a: "It is colour-based, using the image corners as the background sample. It works well for flat studio-like backgrounds; complex backgrounds still need a dedicated segmentation model or manual editing."

curl-converter:
  about: >-
    Convert a curl command into JavaScript fetch, Axios or Python requests code.
    It handles the common method, URL, header and body flags locally in the browser
    so you can move from a copied API example to app code without asking an AI to
    rewrite the same boilerplate.
  faq:
    - q: "Does it execute the request?"
      a: "No. It only converts command syntax into code. Use the HTTP Request Tester if you want the browser to send the request."

line-comma-formatter:
  about: >-
    Format one-item-per-line lists without opening an editor. Add trailing commas,
    turn lines into CSV, quote each item, build a JavaScript array, or make a SQL
    IN list. Empty-line skipping and trimming are optional.
  faq:
    - q: "Can it keep blank lines?"
      a: "Yes. Turn off Skip empty if the blank lines are meaningful. Turn off Trim lines if leading or trailing spaces should be preserved."

env-key-sorter:
  about: >-
    Put a .env file in alphabetical order without shuffling the comments that
    belong to each setting. The top comment block stays first, a comment above a
    key travels with that key, and blank lines are closed up. Duplicate keys can
    be collapsed to the last value, and the equals signs can be lined up for a
    tidier read.
  faq:
    - q: "Does it change the values?"
      a: "No. Only whole lines are moved, so spacing, quotes and inline comments inside a value are untouched. Alignment only pads the space before the equals sign."
    - q: "Which duplicate value wins?"
      a: "The last one, which is what most .env loaders use when a key appears twice."
    - q: "Is my file uploaded?"
      a: "No. The file is read, sorted and written in the browser; nothing leaves the page."
    - q: "Does it understand export?"
      a: "Yes. A line like export PORT=3000 is still recognised as the PORT key and keeps its export prefix."

jwt-expiry-editor:
  about: >-
    Load a JWT, edit its iat, nbf and exp claims with date pickers, and re-encode
    the token. Use the quick +15m / +1h / +1d buttons to extend an expiry, then
    re-sign with HS256, HS384 or HS512, or strip the signature entirely for local
    testing. Everything runs in the browser.
  faq:
    - q: "The signature no longer verifies after editing — is that a bug?"
      a: "No. Changing any claim changes the signed data. Re-sign with the same secret and algorithm the issuer used, or the token will be rejected."
    - q: "Can it sign with RS256 or ES256?"
      a: "Not yet. This tool focuses on HMAC secrets for quick testing; use the JWT Encode tool for PEM-based algorithms."

sql-insert-generator:
  about: >-
    Paste a JSON array of objects or simple CSV and get SQL INSERT statements.
    Column names are collected from the rows, values are quoted per dialect, and
    you can emit one statement per row or a single multi-row INSERT.
  faq:
    - q: "How are objects and arrays stored?"
      a: "Nested objects and arrays are serialised to JSON text and escaped as a string, which is the safe default across dialects."

regex-replace-tester:
  about: >-
    Test a find-and-replace before running it. Enter a pattern, flags, a
    replacement string (with $1 group references) and sample text; the preview
    highlights matches and the result box shows the output.
  faq:
    - q: "Why do I get no replacements with a non-global flag?"
      a: "Without the g flag only the first match is replaced, exactly like String.replace in JavaScript."

cron-next-runs:
  about: >-
    Read a five-field cron expression and see the next scheduled runs as real
    dates, in local time or UTC, with a relative label. Useful for checking that
    a schedule means what you think before saving it to a crontab.
  faq:
    - q: "How are day-of-month and weekday combined?"
      a: "Like standard cron: when both fields are restricted, a run matches if either one matches. When both are wildcards, every day matches."

utm-builder:
  about: >-
    Build a campaign URL with the standard utm_source, utm_medium, utm_campaign,
    utm_term, utm_content and utm_id parameters, or paste an existing URL to see
    which UTM values it carries. Existing query parameters are preserved.
  faq:
    - q: "Does it shorten the URL?"
      a: "No. It only adds and reads parameters. Use your analytics platform's shortener if you need a short link."

time-zone-converter:
  about: >-
    Paste a timestamp the way it appears in a log — for example
    "Sep 26, 2026 @ 00:24:20.437" — pick the zone it was written in, and every
    row shows that same instant in another zone. The default source is UTC
    because that is what most servers and container runtimes log, which is
    exactly the thing that is easy to forget when reading an incident. The
    conversion uses the browser's own time zone database, so daylight-saving
    changes are handled at the instant you enter, not by a fixed offset.
  faq:
    - q: "Why is the default source UTC?"
      a: "Most servers, containers and structured loggers write UTC. Setting the default to UTC means a bare timestamp is read the way the machine that wrote it meant it. Switch the source to Local if the log was written on your own machine in local time."
    - q: "Which input formats are understood?"
      a: "ISO-like dates such as 2026-09-26 00:24:20.437, month-name forms such as Sep 26, 2026 @ 00:24:20.437 or 26 Sep 2026 00:24:20, dates without a time, Unix seconds or milliseconds, and anything the browser can parse when it carries an explicit offset such as Z or +07:00. A value with an offset is taken as an absolute instant and the source-zone selector is ignored."
    - q: "How do daylight-saving changes affect the result?"
      a: "The zone offset is looked up for the exact instant you entered, not for today. If a wall time falls in the hour that repeats or is skipped at a DST change, the page resolves it with the offset that is in force at that moment."

docker-logs-grep:
  about: >-
    Paste the output of docker compose logs and filter it like grep. Match with
    context (the equivalent of grep -C, with separate before and after counts),
    show matching lines only, invert the match, count matches, or list unique
    matching lines. Context groups are separated by "--" and line numbers use
    ":" for a match and "-" for context, so the output looks like grep -n -C.
    Everything runs in the page, so log contents never leave the browser.
  faq:
    - q: "How do I get 10 lines before and 5 lines after a match?"
      a: "Leave the grep type on Match with context, set Lines before to 10 and Lines after to 5. That is the same as grep -B 10 -A 5, and grep -C would use the larger of the two."
    - q: "What is the difference between Regex and plain search?"
      a: "With Regex checked, the pattern is a JavaScript regular expression, so error|timeout matches either word and ^api matches the start of a line. With Regex unchecked every character is literal, so a dot matches only a dot."
    - q: "Can I dedupe a noisy log?"
      a: "Yes. Use the Unique matching lines type, which behaves like grep ... | sort | uniq -c and prefixes each distinct line with how many times it appeared. A pattern of . matches every line, so it can dedupe the whole file."

interest-calculator:
  about: >-
    A calculator for the interest methods that are easy to mix up: simple,
    compound, tiered, step-up, floating, flat, effective and annuity. Enter a
    principal, a rate and a tenor and the page shows the formula it used, the
    result as gross, tax and net where tax applies, and a period-by-period
    breakdown. The product presets follow the tax and day-count conventions
    commonly used in Indonesia and are marked ID; everything is computed in the
    browser.
  faq:
    - q: "What is the formula for simple interest?"
      a: "I = P x r x (days / basis), where P is the principal, r the rate per year as a decimal, and basis the day count (365 or 360). The interest is paid once and never earns interest itself, which is how a deposit that pays at maturity works."
    - q: "What is the formula for compound interest?"
      a: "A = P x (1 + r / n)^(n x t), where n is the number of compounding periods per year and t the tenor in years. Each period's interest is added to the balance so the next period earns interest on it. When tax is withheld at each payment, the amount that compounds is the net interest."
    - q: "How are tiered, step-up and floating interest calculated?"
      a: "All three add up per-slice amounts instead of applying one rate to the whole balance. Tiered splits the balance into brackets and computes slice x rate x (days / basis) for each. Step-up and floating apply a list of rates per period and add P x r_i x span_i. The only difference is that step-up rises on a schedule while floating can move either way."
    - q: "What is the difference between flat, effective and annuity?"
      a: "Flat charges interest on the original principal for the whole term: I = P x r x (months / 12), split evenly across the instalments, so the true cost is higher than the headline rate. Effective charges interest on the remaining balance each month: interest = remaining x r / 12, so the interest share falls over time. Annuity keeps the total instalment fixed with A = P x i / (1 - (1 + i)^-n), where i = r / 12 and n is the number of months, while the split shifts from interest to principal."
    - q: "Why does one month not equal one twelfth of the yearly rate?"
      a: "Because the day-count basis decides how a month becomes a fraction of a year. Actual/365 counts a month as 30/365, a little less than a twelfth; 30/360 treats every month as exactly 1/12, a little more. The page lets you switch basis so the choice is visible instead of hidden."
    - q: "Which parts are specific to Indonesia?"
      a: "The product presets and the ID badges: the final tax on deposit and bond interest, the Actual/365 basis, the ARO rollover, and the loan structures quoted as flat, effective or annuity. The formulas themselves are the same everywhere; only the defaults and the naming are local."

log-parser:
  about: >-
    Turn a wall of log text into rows you can read. The page detects the shape of
    the log — JSON Lines, Apache/Nginx access logs (common and combined), syslog
    (RFC 3164 and 5424), nginx error logs, logfmt and plain
    timestamp-level-message lines — then pulls out the time, the level and the
    message, and keeps every other field as its own column. Filter by level,
    search with a substring or a regular expression, and copy or download the
    result as JSON or CSV. Nothing is uploaded.
  faq:
    - q: "How does auto-detect choose a format?"
      a: "It looks at the first 25 non-empty lines and scores each shape: JSON parses, an access-log pattern matches, a syslog header matches, or two or more key=value pairs appear. The highest score wins, and a log that matches nothing is treated as plain text. Pick the format by hand if the first lines are misleading."
    - q: "What happens to a line that does not match the chosen format?"
      a: "It falls back to plain text instead of being dropped, and the summary counts it as an unparsed line. That way a stray stack trace or a blank banner line is still visible rather than silently vanishing."
    - q: "How are timestamps normalised?"
      a: "Recognised timestamps become ISO 8601 so they sort and compare. Access-log times like 10/Oct/2000:13:55:36 -0700, nginx 2026/09/26 00:24:20, syslog Oct 11 22:14:15 and 10/13-digit epochs are all understood. A timestamp with no year uses the current year."
    - q: "How are log levels mapped?"
      a: "WARN and WARNING become warn, ERR and ERROR become error, CRIT/CRITICAL/FATAL/ALERT/EMERG/PANIC become fatal, and TRACE/DEBUG/INFO/NOTICE map to themselves. Access logs use the status code: 5xx is error, 4xx is warn, everything else is info. Syslog RFC 5424 levels come from the priority value."

date-calculator:
  about: >-
    Two calculators in one page. The difference mode measures the gap between two
    dates in calendar years, months and days as well as total days, weeks, months
    and years, with an option to include the end date and to count business days
    only. The add/subtract mode shifts a date by a number of days, weeks, months,
    years or business days and reports the weekday, ISO week and day of the year.
  faq:
    - q: "Why is the calendar difference not just the total days?"
      a: "A month is not a fixed number of days, so 31 Jan to 1 Mar is one month and one day, not 29 days. The calendar figure keeps the day of the month and clamps to the last valid day when a month is shorter, which is what people mean by a one-month difference."
    - q: "Does including the end date change the calendar difference?"
      a: "No. Include the end date adds one day to the total-day counts, which is what you want for a stay that starts and ends on the listed days, but the years/months/days figure still describes the gap between the two dates themselves."
    - q: "How are business days counted?"
      a: "Weekends (Saturday and Sunday) are skipped. No public-holiday calendar is applied, because holidays differ by country and year, so subtract them yourself if it matters."
    - q: "What happens when I add a month to 31 January?"
      a: "The day clamps to the last day of the target month, so 31 Jan + 1 month is 28 February, or 29 February in a leap year. Adding one month twice is not the same as adding two months, which is why the page does it in one step."

dockerfile-builder:
  about: >-
    Fill in a form and get a Dockerfile. Start from a preset for Node, Python,
    Go, a static site on Nginx, PHP or a Java Maven build, or from scratch, then
    adjust the base image, workdir, ENV, ARG, COPY, RUN, EXPOSE, VOLUME, USER,
    LABEL, HEALTHCHECK, ENTRYPOINT and CMD. Multi-stage builds get a builder
    stage and copy the artifacts into the runtime image. The output is built in
    the page and can be copied or downloaded as a Dockerfile.
  faq:
    - q: "When is a multi-stage build worth it?"
      a: "When the tools that build the app are not the tools that run it: a Go binary needs the Go toolchain only at build time, and a Node app needs dev dependencies only to bundle. The runtime image then carries just the artifact, which is smaller and has fewer things to patch."
    - q: "What is the difference between ENTRYPOINT and CMD?"
      a: "ENTRYPOINT is the program that always runs; CMD supplies default arguments that a docker run command can override. If you set both, put the executable in ENTRYPOINT and the defaults in CMD. The page writes either as JSON when the value starts with [, otherwise as shell form."
    - q: "Why order the instructions this way?"
      a: "Docker caches one layer per instruction and rebuilds from the first changed layer onward. Copying dependency manifests and installing before copying the rest of the source means a code change does not re-install dependencies. Keep the slow, rarely-changing steps near the top."
    - q: "COPY or ADD?"
      a: "The page emits COPY, which is what you almost always want: it copies files and directories. ADD also unpacks local tarballs and can fetch URLs, which is convenient but surprising, so use it deliberately rather than by default."

nginx-reverse-proxy-wizard:
  about: >-
    A guided way to write a reverse proxy server block. Step through the domain,
    the upstream pool (one or more hosts, a balancing method and keepalive), TLS
    with a plain-HTTP redirect, and the extras an app usually needs — WebSocket
    upgrade, streaming, security headers and upload size. The config is built live
    beside the steps and can be copied or downloaded.
  faq:
    - q: "Why is there an upstream block instead of a plain proxy_pass?"
      a: "An upstream block lets nginx pool several backends, apply a balancing method, and reuse connections with keepalive. A bare proxy_pass to one host opens a new connection per request. The wizard always writes an upstream so the config can grow without being rewritten."
    - q: "Why does the WebSocket option add a map block?"
      a: "nginx cannot choose between the upgrade and close values inline, and the Connection header has to match the request. The map block turns $http_upgrade into $connection_upgrade once, and the location uses it. Without the map, the header would be wrong for either WebSockets or ordinary requests."
    - q: "Does the app see the real client IP?"
      a: "The location forwards X-Real-IP, X-Forwarded-For and X-Forwarded-Proto, so the app can read the original client and scheme. Make sure the app trusts those headers only from nginx, or a client could spoof them."
    - q: "Why skip keepalive when the target is https?"
      a: "Keepalive to an upstream needs HTTP/1.1 over a plain connection to reuse cleanly. An https target means a TLS handshake per connection, so the wizard leaves keepalive out and says so rather than pretending it helps."

typing-speed-test:
  about: >-
    A focused typing test. Pick a time goal (15, 30, 60 or 120 seconds) or a
    word goal (10 to 100 words), optionally add punctuation and numbers, then
    start typing. The timer starts on your first keystroke, every character is
    coloured as you go, and the result panel reports net and raw WPM, accuracy,
    consistency, character counts and total time. Everything runs on your device
    and nothing is uploaded.
  faq:
    - q: "How is WPM calculated?"
      a: "Net WPM is correct characters divided by five, divided by the minutes elapsed. Five characters is the standard 'word'. Raw WPM uses every character you typed, correct or not, so the gap between the two numbers shows how much accuracy is costing you."
    - q: "What is consistency?"
      a: "It is how steady your speed was, not how fast. The page samples raw WPM once per second and reports 1 minus the coefficient of variation as a percentage. A high number means an even pace; a low number means bursts and stalls."
    - q: "How does accuracy differ from the red characters?"
      a: "The red characters show the current state of the text, so fixing a mistake turns a character back to normal. Accuracy is counted from keystrokes: every character you press is recorded, and a wrong press stays wrong even after you backspace and fix it. That is why a text with no red can still read below 100%."
    - q: "Why does the test not start until I type?"
      a: "So the clock measures typing, not reading. Nothing happens until the first keystroke, and the word goal ends the moment you reach the last character. Press Enter after a finished test, or the Restart button, to go again with fresh words."

json-path-explorer:
  about: >-
    A JSONPath workbench. Paste a JSON document, type an expression such as
    $..author or $.store.book[?(@.price < 10)].title, and the page lists every
    match with the exact path that reaches it. The tree below the editor shows
    the whole document: click any key to load that value's path into the
    expression box, which is the quickest way to learn the syntax. The
    JSONPath engine is vendored and runs entirely in the page.
  faq:
    - q: "What does a JSONPath expression look like?"
      a: "It starts at the root with $, then walks the document. A dot reads a key ($.store), a bracket reads an index or a quoted key ($.store.book[0], $['a key']), [*] is every item, .. searches at any depth, and [?(@.price < 10)] filters. You can mix them, for example $..book[?(@.price < 10)].title."
    - q: "Why does clicking a key change my expression?"
      a: "That is the explorer part: each key in the tree carries the path that reaches it, and clicking it loads that path so you can see it work, then extend it by hand. It is a way to build a long path without counting brackets."
    - q: "Are filters and recursive search supported?"
      a: "Yes. The vendored engine supports recursive descent (..), wildcards ([*]), array and object unions, slices ([1:3]), and filter expressions with comparisons such as ==, !=, <, <=, > and >=. If an expression is not valid, the status line says so rather than showing a wrong result."
    - q: "What does the path in the results mean?"
      a: "It is the canonical JSONPath that reaches that match, always written with brackets. It is useful when you want to copy one exact location out of a large document, or reuse it in another tool that takes a JSONPath."
    - q: "Is my JSON uploaded?"
      a: "No. Parsing, matching and the tree all run in your browser, and nothing is sent to a server."

json-schema-validator:
  about: >-
    Paste a JSON Schema and a JSON document and the document is checked against
    it in the page. The validator covers the parts of draft-07 that carry most
    real API contracts: type, enum, const, numeric and string bounds, arrays and
    objects, required and additional properties, $ref, allOf/anyOf/oneOf/not and
    if/then/else. Every problem is reported at once, with the JSON Pointer path
    that leads to it, so a document can be fixed in a single pass.
  faq:
    - q: "Does my data leave the browser?"
      a: "No. Both the schema and the document are parsed and checked in the page. Nothing is uploaded, which is the point of a tool like this when the payload is a real API response."
    - q: "Which draft of JSON Schema is supported?"
      a: "A practical subset of draft-07. The common validation keywords are implemented; annotations such as title, description and default are ignored because they do not change whether a value is valid. Remote $ref targets are not fetched — only local references into the same schema."
    - q: "Why do I see several errors for one mistake?"
      a: "Each keyword is checked independently, so a wrong type can also trip a pattern or an enum further down. Fixing the first error usually clears the ones that follow, which is why they are listed together instead of one at a time."
    - q: "What is the path next to each message?"
      a: "A JSON Pointer into the document, starting at # for the root, with each property and array index appended. It points straight at the value that failed, so you can copy it into code that walks the same path."
    - q: "Is a valid result a guarantee?"
      a: "It means the document satisfies the keywords this page implements. It is not a full conformance suite: unknown keywords are treated as annotations, and formats are checked with pragmatic rules rather than the strictest reading of every specification."

game-2048:
  about: >-
    The classic 2048 puzzle, rebuilt as a single page. Slide the board with the
    arrow keys, WASD or a swipe; equal tiles merge into one and a fresh tile
    appears after every move. The current score, the best score on this device
    and a one-step undo sit above the board. The game is won when a 2048 tile
    appears, but play continues for a higher score. Everything runs locally and
    nothing is uploaded.
  faq:
    - q: "How does merging work?"
      a: "A move slides every tile as far as it will go in that direction. Two tiles of the same value that meet merge into one tile of double the value, and a tile that has already merged once in a move cannot merge again. That single-merge rule is why sliding left can pair the first two tiles but leave the third of a triple alone."
    - q: "Where is my best score kept?"
      a: "In this browser's local storage, under a key that belongs to this page. It survives reloads and never leaves your device. Clearing site data resets it."
    - q: "What does undo do?"
      a: "It steps back exactly one move, including the score you gained and the random tile that appeared. There is only one level of undo, so use it to recover from a mis-swipe rather than to rewind a whole game."
    - q: "Why did the game end when I still had empty squares?"
      a: "A game is over only when no direction can move any tile. That can happen with gaps left on the board if the remaining tiles are all different and boxed in, so the board can look half-empty and still be finished."
    - q: "Is there a strategy?"
      a: "Most strong players keep their largest tile in one corner and build a descending chain along two edges, never moving the corner tile away. Deciding early which corner that is matters more than any single merge."

memory-match:
  about: >-
    A memory game with three board sizes. Cards start face down; flip two at a
    time and a matching pair stays up. The page counts your moves and times the
    round from the first flip to the last pair, so you can see whether a bigger
    board costs you speed or accuracy. Symbols are shuffled on every new game.
  faq:
    - q: "What do the difficulty levels change?"
      a: "Only the number of pairs. Easy is a 4 × 4 board with eight pairs, Medium is 6 × 4 with twelve, and Hard is 6 × 6 with eighteen. More pairs mean more to remember at once, not a stricter rule set."
    - q: "When does the timer start?"
      a: "On your first flip, not when the page loads, so thinking time before you begin is not counted. It stops the moment the final pair is matched."
    - q: "Why did my two cards turn back over?"
      a: "They were not a pair. A mismatch is shown for a moment so you can memorise the symbols, then both cards turn face down again. A matching pair is locked face up."
    - q: "Does a new game reshuffle the same symbols?"
      a: "Yes, the same set of symbols is reused but their positions are shuffled every time. Switching difficulty also deals a fresh board immediately."
    - q: "Is the board different every time?"
      a: "The shuffle is random on each new game, so no two boards are the same unless you are very unlucky. Nothing about the board is stored or shared."

minesweeper:
  about: >-
    Minesweeper with first-click safety and three board sizes. Click a cell to
    open it and right-click to flag a suspected mine. Opening an empty cell
    clears its neighbours automatically, numbers count the mines touching a
    cell, and the round is won when every safe cell is open. A timer and a
    mines-left counter sit above the grid.
  faq:
    - q: "What does first-click safety mean?"
      a: "The mines are placed only after your first click, and never on that cell or its eight neighbours. Your opening move can therefore never end the game and will always clear a small area to work from."
    - q: "How do I flag a mine?"
      a: "Right-click a closed cell to flag it, and right-click again to remove the flag. Flagging is a note to yourself: it does not open the cell and it does not affect whether you win. The counter shows how many mines are still unaccounted for."
    - q: "What do the numbers mean?"
      a: "A number is how many of the eight surrounding cells contain a mine. The colour of the number changes with its value so a busy cell stands out at a glance."
    - q: "How do I win?"
      a: "Open every cell that is not a mine. You do not need to flag every mine, and you can finish with flags still on the board — the win is based on the safe cells you have cleared."
    - q: "Is the board solvable without guessing?"
      a: "Not always. Minesweeper can produce positions where logic alone leaves two equally likely options, and no amount of care will avoid a guess. The difficulty presets set the mine density, not a guarantee of a purely deductive board."

snake:
  about: >-
    A grid Snake that runs entirely in the page. Steer with the arrow keys or
    WASD, or the on-screen pad on touch. The snake starts on your first turn,
    grows by one each time it eats, and speeds up as the score rises. The game
    ends on a wall or on the snake's own body, and Space pauses. The best score
    is kept on this device.
  faq:
    - q: "When does the snake start moving?"
      a: "Only after your first direction, so the page never starts a game you did not ask for. After that the snake keeps moving in its current direction until you turn or pause."
    - q: "Why can't I reverse direction?"
      a: "Turning straight back into your own neck would end the game instantly, so a 180-degree turn is ignored. Press a perpendicular direction first and then the reverse if you really need to double back."
    - q: "How does the speed work?"
      a: "Each food makes the step delay a little shorter, down to a floor so the game stays playable. That means the longer you survive, the less time you have to react — the difficulty is the growth, not a separate setting."
    - q: "Does eating count on the tail cell?"
      a: "No. The cell the tail is about to leave is treated as free, so following your own tail closely is safe as long as you keep moving. Only the body that stays behind blocks you."
    - q: "Where is my best score stored?"
      a: "In this browser's local storage, tied to this page. It is never uploaded and disappears if you clear site data."

devops-tycoon:
  about: >-
    A browser-based infrastructure management game. You run an internet
    service with one application server and a small budget. Traffic grows on
    its own, every served request earns money, and servers, bandwidth and the
    database all cost money to keep running. Buy capacity before the load
    outruns it, and answer incidents — CPU overload, disk full, database
    overload, traffic spikes, crashes — before users give up. The simulation
    is a simple one-second tick and everything runs on this page.
  faq:
    - q: "How does the game decide how much money I make?"
      a: "Every successful request earns a fixed amount. The error rate rises when CPU or the database pass their limits, so overloaded infrastructure earns less while still costing the same to run. Growing traffic raises revenue, which is why the shop has to keep up with it."
    - q: "Why does adding servers not help as much as I expected?"
      a: "Without a load balancer, extra application servers are used inefficiently, so you only get part of their capacity. Buying the load balancer makes every server count fully. It is the game's way of showing why balancing matters as much as raw capacity."
    - q: "What happens if I ignore an incident?"
      a: "The incident keeps adding to your error rate until it times out, then costs you money and satisfaction. Resolving it with a quick fix is cheap but temporary; buying the matching upgrade fixes the cause for good."
    - q: "What do the temporary fixes do?"
      a: "Restarts and similar actions lower the error rate for a few ticks, buying you time to afford a permanent fix. They do not change your capacity, so the same incident can return if the underlying load is still too high."
    - q: "Is my progress saved?"
      a: "No. The game runs in memory and resets when you reload the page, so there is nothing to store and nothing to clean up. Nothing is sent anywhere."

tetris:
  about: >-
    A full Tetris in a single page: seven-bag randomiser, next-piece preview,
    hold slot, a faint ghost showing where the piece will land, levels and
    scoring. Arrow keys move and rotate, Down soft-drops, Space hard-drops, C
    holds and P pauses. The board is drawn with DOM cells rather than a canvas,
    so it stays crisp at any size and needs no plugins.
  faq:
    - q: "How does the piece randomiser work?"
      a: "It uses a seven-bag: all seven tetrominoes are shuffled into a bag and dealt one at a time, then a new bag is shuffled. That means you never wait more than twelve pieces for a given shape, which is fairer than pure random drops."
    - q: "What is the ghost piece?"
      a: "The faint outline at the bottom of the falling piece. It shows where the piece will come to rest if you hard-drop right now, so you can judge a placement before committing to it."
    - q: "How is scoring calculated?"
      a: "Clearing one line is 100, two is 300, three is 500 and four is 800, multiplied by the current level. Soft-dropping adds one point per row and a hard drop adds two per row. Level rises every ten lines and speeds the fall up."
    - q: "What does the hold slot do?"
      a: "Press C to park the current piece and bring back whatever was in the hold slot, or to pull the piece out of it if the slot was empty. You can hold only once per piece, so it is a way to defer an awkward shape rather than to cycle freely."
    - q: "Why is the board made of divs instead of a canvas?"
      a: "A DOM grid keeps the theme's flat, crisp look at every zoom level, is selectable by the same styling as the rest of the site and needs no drawing code. It also means the game works even where canvas is unavailable."

fuel-economy-converter:
  about: >-
    A one-in, four-out fuel economy converter. Type a figure, choose the unit it
    is in, and km/L, L/100 km and both MPG figures update as you type. All four
    units are defined by an exact conversion to km/L and back, so a value and its
    round trip agree. The maths runs in the page and nothing is uploaded.
  faq:
    - q: "Why are there two MPG values?"
      a: "Because the US and UK gallons are not the same size. A UK gallon is about 20% larger, so the same car always shows a higher UK MPG figure. The converter keeps both so a review or a spec sheet written in either country can be read without guessing."
    - q: "Which direction is better?"
      a: "Higher km/L and higher MPG are better; lower L/100 km is better. That inversion is the usual reason a plain percentage change is confusing — a 10% drop in L/100 km is an improvement, not a loss."
    - q: "How is L/100 km converted?"
      a: "It is the reciprocal relationship: L/100 km equals 100 divided by km/L, and km/L equals 100 divided by L/100 km. That is why the two curves are not linear and small L/100 km figures turn into very large km/L figures."
    - q: "What are the exact MPG factors?"
      a: "One km/L is 2.352145833 MPG (US) and 2.824809 MPG (UK). Equivalently, MPG (US) is 235.214583 divided by L/100 km and MPG (UK) is 282.480936 divided by L/100 km. Those constants come from the litre-to-gallon definitions and are exact to the digits shown."
    - q: "Is the result rounded?"
      a: "Each figure is shown to three decimal places, which is finer than any dashboard or pump can measure. The underlying value is not rounded before conversion, so the four outputs stay consistent with one another."

ipv6-expander:
  about: >-
    Paste an IPv6 address in any spelling and the page shows the full eight-group
    form, the canonical compressed form and what kind of address it is. Prefixes,
    zone indexes and embedded IPv4 addresses are understood, and everything is
    computed in the browser.
  faq:
    - q: "Why does the compressed form sometimes keep a zero group?"
      a: "The canonical rules compress only the longest run of two or more zero groups. A single zero group is written as a plain 0, and on a tie the leftmost run is the one compressed, so the same address always has one canonical spelling."
    - q: "What is the difference between link-local, unique local and global?"
      a: "Link-local (fe80::/10) is for one link and is never routed. Unique local (fc00::/7) is the IPv6 equivalent of private space, routable inside an organisation but not on the public internet. Global unicast (2000::/3) is the public, routable range."
    - q: "What is an IPv4-mapped address?"
      a: "An address of the form ::ffff:a.b.c.d that carries an IPv4 address inside an IPv6 one. It appears on dual-stack sockets and in logs, and the expander recognises it so you are not left guessing why a dotted quad shows up in an IPv6 field."
    - q: "Does the zone index change the address?"
      a: "No. The part after % names a local interface, such as %eth0, and is not part of the 128 bits. It matters only to the host you are on, which is why it is shown separately and not included in the expanded or compressed forms."

http-header-parser:
  about: >-
    Paste a raw header block from curl, a proxy log or the browser network panel
    and the page turns it into a clean table. A status line is optional, folded
    continuation lines are rejoined, and repeated fields such as Set-Cookie are
    kept and marked so nothing is silently dropped.
  faq:
    - q: "Why are some headers marked as repeated?"
      a: "Because the same field name appears more than once. Some headers, like Set-Cookie, are supposed to repeat and must not be merged; others are combined by the recipient. The label shows you which is which before you act on the values."
    - q: "How are folded lines handled?"
      a: "Older systems wrapped long header values onto the next line with leading whitespace. The parser treats a line that starts with a space or tab as a continuation of the previous header and joins it back with a single space, which is what the HTTP rules say to do."
    - q: "What is the status line?"
      a: "The HTTP/1.1 200 OK line that opens a response. It is optional here, so you can paste just the headers, but when it is present it is reported separately and not mistaken for a header."
    - q: "Is anything sent over the network?"
      a: "No. The text you paste is parsed in the page. That is deliberate: header blocks often carry cookies, tokens and internal hostnames that should not leave your machine."

http-header-builder:
  about: >-
    A small workbench for building a header block. Type one Name: value pair per
    line, tick any common security headers you want added, and the page validates
    the names, keeps the last value for a repeat and emits a clean block ready to
    paste into a request or a server config.
  faq:
    - q: "What makes a valid header name?"
      a: "A header name is an HTTP token: letters, digits and a small set of symbols, with no spaces or colons. The builder checks each name against that rule and reports any line it cannot use instead of guessing what you meant."
    - q: "What happens if I list the same header twice?"
      a: "The last value wins. That matches how most tools and servers resolve a duplicated field, and it means you can put a placeholder near the top and override it further down without editing the earlier line."
    - q: "Which security headers can I add?"
      a: "HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy, a starter Content-Security-Policy, Permissions-Policy and a no-store Cache-Control. They are added only when you have not already set that header yourself, so your own value is never overwritten."
    - q: "Does the page send the headers anywhere?"
      a: "No. It is a text builder: the input is parsed and the output is written back into the page. Nothing is transmitted, which is safe even when the block contains an Authorization value."

websocket-frame-parser:
  about: >-
    Paste the hex bytes of a single WebSocket frame and the page decodes the bits
    the RFC defines: FIN, the reserved bits, the opcode, whether the frame is
    masked, the payload length, the masking key and the payload itself. Text
    frames are unmasked and shown as readable text.
  faq:
    - q: "Where do I get the raw frame bytes?"
      a: "From a packet capture or a proxy that logs frames, such as Wireshark with a WebSocket filter, or from a test that dumps the bytes your client sent. Copy the frame payload bytes, not the TCP or TLS wrapper, and paste them as hex."
    - q: "Why is the payload scrambled?"
      a: "Client-to-server frames are masked, so the bytes on the wire are XORed with a four-byte key. The parser applies that key to recover the payload. A server-to-client frame is not masked and is shown as it arrived."
    - q: "What does the opcode mean?"
      a: "0x1 is text, 0x2 is binary, 0x0 continues a fragmented message, 0x8 closes the connection, 0x9 is a ping and 0xa is a pong. Anything else is reserved and is labelled as such rather than guessed at."
    - q: "Does this connect to a server?"
      a: "No. It reads bytes you paste and nothing else. There is no socket and no request, so it is safe for frames that contain tokens or message payloads from production."

port-reference:
  about: >-
    A compact, offline table of the ports that come up most often when you read
    configs, logs and firewall rules. Each row gives the port number, whether it
    is TCP, UDP or both, the usual service name and a one-line note on what it
    carries. Search by number or name.
  faq:
    - q: "Is this a full list of every port?"
      a: "No, it is a shortlist of the common ones. The IANA registry has thousands of assignments. This table is meant to answer the everyday question of what is probably running on a port, not to be a complete authority."
    - q: "Does a port number guarantee the service?"
      a: "No. A port is just a number; anything can listen on it. The service column shows the conventional assignment, so treat it as a strong hint and confirm with the process or the application config."
    - q: "What is the difference between TCP and UDP for the same port?"
      a: "They are separate namespaces. Port 53 is used by DNS over both, and port 123 is UDP for NTP. When a service uses both, the row lists tcp/udp, and the protocol filter matches either."
    - q: "Why are some dev-server ports listed as alternatives?"
      a: "Ports like 3000, 5000 and 8000 have no official service. They are shown as common development defaults because that is how they actually turn up, and the note makes clear they are conventions rather than assignments."

email-header-analyzer:
  about: >-
    Paste the raw headers of a message and the page lays out the delivery path:
    each Received hop in order, the hosts and IP addresses involved, the time
    between hops, and the SPF, DKIM and DMARC verdicts from the
    Authentication-Results header. Nothing leaves the browser.
  faq:
    - q: "Why do the hops appear in a different order from the paste?"
      a: "Mail servers prepend a Received header, so the first one in the file is the most recent hop. The analyzer reverses them so hop 1 is where the message started and the last hop is where it arrived, which is the order the journey happened in."
    - q: "How is the delay between hops worked out?"
      a: "Each Received header ends with a timestamp after the semicolon. The page parses those timestamps and subtracts consecutive ones, so a large gap points at a server that queued the message. Clock skew between servers can make a delay look negative."
    - q: "What do the SPF, DKIM and DMARC values mean?"
      a: "They are the receiving server's verdicts. pass means the check succeeded, fail means it did not, and softfail, neutral, none or temperror are the softer outcomes. They come from Authentication-Results and are reported exactly as written."
    - q: "Can I use this to trace spam?"
      a: "It helps. The earliest hop and the sending IP are the most useful, and the authentication verdicts show whether the sender was allowed to use the domain. Treat the headers as evidence to check, not proof, since earlier hops can be forged."

open-graph-debugger:
  about: >-
    Paste a page's HTML and the page pulls out its Open Graph and Twitter Card
    tags, the title and the canonical link, then draws a preview of the card a
    link would produce. It is the quick way to see why a shared link shows the
    wrong title or no image.
  faq:
    - q: "Why does the preview look different from the real one?"
      a: "Every platform renders the card its own way and may crop the image or ignore a tag. This preview shows the values that were found and the order of priority, which is what you need to debug, not a pixel-perfect copy of any one site."
    - q: "Which title wins if several are present?"
      a: "Open Graph is preferred over Twitter Card, and both are preferred over the plain title element. That matches how most crawlers resolve it, and the tag table still lists every value so you can see what else is there."
    - q: "The image is missing from the preview. Why?"
      a: "The parser only reads the tag; it does not fetch the image. If og:image is absent or empty, the card shows no image. Use an absolute URL, since relative image paths are unreliable for crawlers."
    - q: "Is the page fetched for me?"
      a: "No. You paste the HTML and it is parsed in the browser with a local DOM parser. Nothing is requested, so you can inspect a staging page behind a login or a page that is not public yet."

network-info:
  about: >-
    A snapshot of the connection and device facts a browser exposes to a page:
    online state, the Network Information values where they exist, screen and
    viewport size, pixel ratio, language, CPU threads, memory hint and time zone.
    It is read locally and never uploaded.
  faq:
    - q: "Why are some values blank?"
      a: "Support varies. The Network Information API, deviceMemory and hardwareConcurrency are not in every browser, and some are rounded or capped for privacy. A dash means the browser did not provide a value, not that it is zero."
    - q: "How accurate is the downlink and round-trip time?"
      a: "They are estimates the browser derives from recent traffic and are deliberately coarse. They are fine for choosing an image size or deciding whether to prefetch, but not for measuring a connection."
    - q: "Does this reveal my IP address or location?"
      a: "No. The page only reads values the browser already exposes to JavaScript. It does not call an IP service, does not use geolocation and does not make a network request."
    - q: "Why does the time zone matter?"
      a: "It is handy when you are debugging a timestamp bug and need to know what the browser thinks local time is. It comes from Intl, so it reflects the system setting rather than anything you typed."

api-mock-response-builder:
  about: >-
    Describe a response shape once as name: type pairs and the page generates a
    realistic JSON body with the number of records you ask for. It is the quick
    way to stub an endpoint, seed a fixture or show a frontend what the real
    payload will look like before the API exists.
  faq:
    - q: "What types can I use?"
      a: "string, word, name, firstname, lastname, email, phone, city, country, address, company, product, url, image, uuid, id, number, integer, price, age, boolean, date, datetime, timestamp, color, object and array(n). Anything else is reported and skipped rather than guessed."
    - q: "How do I get a list of values in one field?"
      a: "Write array(3) for three items or array for a default of three. Each item is a short word, which is enough to fill a tags or categories field while you build the UI."
    - q: "Are the values random every time?"
      a: "Yes. Each render draws new values, and the Regenerate button does the same. If you need the exact same payload twice, paste the generated JSON into a file instead of regenerating."
    - q: "Is the data based on anything real?"
      a: "No. Names, addresses and emails are made up from small built-in word lists. They are deliberately fake so nothing generated can collide with a real person or mailbox."

webhook-tester:
  about: >-
    A webhook workbench that runs entirely in the browser. Build a payload,
    sign it with HMAC-SHA256 in the format a provider expects, paste a
    signature you received to check it, or send the signed request to a test
    endpoint. The signing secret stays on your machine and is never uploaded.
  faq:
    - q: "Which providers are supported?"
      a: "Stripe, GitHub, Shopify and a generic HMAC-SHA256 format. Each one signs a different string and wraps the result differently: Stripe signs the timestamp and payload and writes t= and v1=, GitHub writes sha256=, Shopify sends base64, and the generic format is plain hex."
    - q: "Why is the timestamp part of the signature?"
      a: "It lets the receiver reject an old request that someone replays later. Stripe includes the timestamp in the signed string and sends it alongside, so changing the timestamp invalidates the signature. Use the Now button to refresh it before sending."
    - q: "Why does sending fail with a network error?"
      a: "Browsers can only send a request when the target allows your origin through CORS, and a webhook endpoint usually does not. That is a browser rule, not a bug. The signature panel still shows exactly what would be sent, so you can test the signing even when the send is blocked."
    - q: "Is the secret safe?"
      a: "It never leaves the page: the HMAC is computed locally with the Web Crypto API and there is no upload step. Still, treat any secret you paste into a website as one you control, and prefer a test secret over a live one."

snap-signature:
  about: >-
    A workbench for the asymmetric signature in Bank Indonesia's SNAP BI
    standard, the scheme banks and payment providers use for API requests and
    notifications. It builds the string to sign from the HTTP method, endpoint,
    a SHA-256 hash of the minified body and the X-TIMESTAMP, then signs it with
    an RSA private key or verifies it with the sender's public key. Both
    operations run in the browser with the Web Crypto API.
  faq:
    - q: "What exactly is the string that gets signed?"
      a: "HTTPMethod + ':' + EndpointUrl + ':' + lowercase(hex(SHA-256(minify(body)))) + ':' + X-TIMESTAMP. The method is upper-cased, the body is minified JSON, and its SHA-256 is written as lowercase hex. Join the four parts with colons and sign that exact string."
    - q: "Which key goes where?"
      a: "Signing uses the private key of the sender. Verification uses the public key of the sender, not yours. For a notification you received, the sender is the payment provider, so paste the public key they published."
    - q: "Why is the body minified before hashing?"
      a: "The signature must not depend on cosmetic whitespace, so both sides agree to strip it first. JSON.stringify(JSON.parse(body)) removes the formatting and the receiver hashes the same compact form. If the body is not valid JSON it is hashed as typed."
    - q: "Is this the same as SHA256withRSA in Java or Node?"
      a: "Yes. Web Crypto's RSASSA-PKCS1-v1_5 with SHA-256 is exactly SHA256withRSA, so a signature made here verifies in Java, Node or any SNAP BI library, and the other way round."
    - q: "Can I use a production private key here?"
      a: "Technically yes, but do not. This is a public web page, so paste a test key, or press Generate key pair to create a throwaway 2048-bit pair and try the flow end to end."

javascript-playground:
  about: >-
    A console for trying JavaScript without leaving the page, built on the
    Monaco editor — the same editor that powers Visual Studio Code — so the code
    gets syntax highlighting, bracket matching, code folding and a formatter.
    Write or paste a snippet, press Run, and see everything it logs, the value
    it returns and how long it took. Monaco is a large bundle, so it is fetched
    from a CDN only when you press Enable Monaco; until then, and if the fetch
    fails, the page uses a plain text area with the same run, copy, download and
    clear actions. Nothing you type is ever sent anywhere.
  faq:
    - q: "Why does the Monaco editor not appear straight away?"
      a: "Monaco is several megabytes, so loading it on every visit would slow the page down for people who only want a quick text box. It is fetched on demand when you press Enable Monaco. Until then a plain text area is used, and it keeps working if the fetch fails."
    - q: "Can I use top-level await?"
      a: "Yes. The snippet is wrapped in an async function, so await works at the top level and return gives back a value. That also means you do not need to wrap everything in an async IIFE yourself."
    - q: "Is the code sandboxed?"
      a: "No. It runs in the page with the page's own privileges, exactly like the browser's developer console. That keeps it simple and lets it touch real APIs, but it also means you should only run code you trust."
    - q: "Why did my console.log output not appear?"
      a: "Only output produced while the snippet runs is captured, and the snippet has to finish for the run to end. If it throws, the error is shown instead and anything logged before the throw is still listed."
    - q: "Can I edit other languages?"
      a: "The playground is for JavaScript, so the editor stays in JavaScript mode. The plain text area still holds whatever you paste, but the highlighting and the formatter are the JavaScript ones."
    - q: "Does it remember my code between visits?"
      a: "The editor starts from a small example each time. If you want to keep a snippet, use Copy before you leave, or paste it into your own notes."

linux-ops:
  about: >-
    A troubleshooting companion for Linux servers. Describe the problem in
    plain language — which process is using port 8080, disk is full, nginx
    returns 503 — and it finds the matching command, explains every flag, rates
    how risky it is and suggests what to check next. There is also an analyzer:
    paste the output of df, free, ss, docker ps, nginx -t, systemctl status or
    journalctl and it reads the output back to you. Everything runs in your
    browser; nothing is uploaded, and a search that looks like it contains a
    secret is kept out of the URL.
  faq:
    - q: "Does it run the commands for me?"
      a: "No. It only suggests commands and explains them; you copy the one you want and run it yourself. That is deliberate: a tool that can run anything on your server is a tool that can break your server."
    - q: "Is my search sent anywhere?"
      a: "No. The catalog, the search index and the analyzer all run in your browser. Nothing is uploaded, and if a search looks like it contains a secret — a token, a password, a private key — it is kept out of the URL too."
    - q: "What can the analyzer read?"
      a: "Paste the output of df, free, ss, docker ps, nginx -t, systemctl status, lsblk, git status, ps, top, pvs, or MySQL and PostgreSQL status output. It detects the format, points out the lines that matter and suggests the next command."
    - q: "Are the suggested commands safe?"
      a: "Each command is labelled safe, changes state, or dangerous, and destructive ones are explained before you run them. Still read the command and its flags before pasting it into a production shell."

ssh-tunnel-builder:
  about: >-
    Write an ssh tunnel without memorising the -L, -R and -D shapes. Pick the
    direction, fill in the ports, and the page assembles the command with the
    keepalive and failure options most people forget. The bind address is
    optional, and -D hides the destination fields because a SOCKS proxy has
    none.
  faq:
    - q: "What is the difference between -L, -R and -D?"
      a: "A local forward (-L) makes a service on the far side reachable from a port on this machine. A remote forward (-R) does the opposite: it publishes a service on this machine to the far side. A dynamic forward (-D) opens a SOCKS proxy you can point a browser at."
    - q: "Why are -f and -N on by default?"
      a: "Together they put the tunnel in the background and run no remote command, which is what you usually want for a tunnel that only forwards ports. Turn them off if you also want a shell on the host."
    - q: "Does this connect for me?"
      a: "No. It only writes the command; you copy it and run it in your own terminal, so no key or password ever touches the page."

sed-replacement-builder:
  about: >-
    Build the sed command you would otherwise look up again. Choose between a
    substitution, deleting matching lines or printing only the matches, then set
    the regex style, the delimiter and the flags. The page escapes the delimiter
    inside the pattern and quotes the whole expression, so a copy-paste into the
    shell behaves the same way it did here.
  faq:
    - q: "Is an in-place edit reversible?"
      a: "Only with the backup. With -i.bak on, sed leaves the original next to the file with a .bak suffix; turn the backup off and there is no undo."
    - q: "What does the I flag do?"
      a: "It makes the pattern match without regard to case. It is a GNU extension, so it works with GNU sed on Linux but not with the old BSD sed on macOS."
    - q: "Why does it quote the expression in single quotes?"
      a: "A sed expression is full of characters the shell would otherwise interpret, like spaces, pipes, stars and dollar signs. Single quotes hand the whole expression to sed untouched."

grep-pattern-builder:
  about: >-
    Put together a grep command with the right regex flavour and the flags that
    keep the output readable. Pick extended, basic, fixed or Perl patterns,
    choose recursion, case, line numbers, context and file filters, and the page
    builds the command with the pattern safely quoted.
  faq:
    - q: "When should I use -F?"
      a: "When you are searching for literal text that contains regex characters, such as an IP address with dots or a string with brackets. -F treats the pattern as plain text, so nothing needs escaping."
    - q: "What is the difference between -E and -P?"
      a: "-E uses POSIX extended regular expressions, which are portable and cover word boundaries, groups and alternation. -P uses PCRE for lookaround and lazy quantifiers, but it is a GNU feature and not available on every system."
    - q: "How do I pass several extensions?"
      a: "Type one include pattern at a time, or a shell expansion such as a brace list you write yourself. The builder writes one --include for whatever you enter."

find-command-builder:
  about: >-
    Build a find command from plain choices instead of remembering the test
    order. Filter by name, type, size, age, permissions and depth, exclude noisy
    directories, and then decide what happens to each match: print it, print it
    with a null separator, delete it, or run a command on it.
  faq:
    - q: "Why does -delete deserve caution?"
      a: "It removes matches immediately and asks nothing. Run the same command with the print action first so you can see the exact list, then switch to delete."
    - q: "What is the null-separated option for?"
      a: "-print0 ends each path with a null byte instead of a newline, so a name that contains a space or a newline is still one item. Pair it with xargs -0."
    - q: "Does the order of the options matter?"
      a: "Yes. -maxdepth is a global option and has to come before the tests, and -delete has to come last. The builder already places them in a working order."

xargs-builder:
  about: >-
    Build an xargs command and see what each flag means. Choose how the input is
    split, how many items go to each command, how many commands run at once, and
    whether a replace string is used. The page writes the command and lists a
    plain-language note for every flag it added.
  faq:
    - q: "Why pair -0 with find -print0?"
      a: "The null byte is the only character that cannot appear in a file name, so find -print0 and xargs -0 pass paths with spaces or newlines through without splitting them into extra arguments."
    - q: "What does -I do to -n?"
      a: "-I implies one item per command, so -n has no effect once -I is set. The builder drops -n when you turn on the replace string."
    - q: "Is it safe to run commands in parallel?"
      a: "Only if the command does not depend on order and does not write to the same output. -P speeds things up, but two jobs writing the same file will race."

currency-formatter:
  about: >-
    Turn a plain number into a readable money amount, or the other way round.
    Pick the symbol, the separator style and how many decimals to show, and the
    page writes the formatted value. It also reads a value you paste, so
    1.250.000 and 1,250,000.50 both come back as numbers before being
    reformatted the way you want.
  faq:
    - q: "Why do the separators matter?"
      a: "Indonesian uses a dot for thousands and a comma for decimals, the reverse of English. Sending 1.250 to a reader who expects English looks like one and a quarter, so the style switch keeps the amount unambiguous."
    - q: "What does the decimal places box do?"
      a: "It pins the number of digits after the decimal mark. Leave it blank and whole numbers show none while fractional ones show two; set it to 0 to round rupiah to the nearest whole unit."
    - q: "Does it add a currency code?"
      a: "No. It only knows the symbol you pick, so Rp and $ are distinguished by shape alone. Use a code such as IDR or USD when a machine has to read the value."

number-to-words:
  about: >-
    Write an amount in Indonesian words, the way it is done on an invoice or a
    cheque. Enter a number and the page spells it out, grouping by ribu, juta,
    miliar and triliun, with an optional capital first letter. Decimals are read
    one digit at a time after the word koma.
  faq:
    - q: "How is 1,250,000 read?"
      a: "As satu juta dua ratus lima puluh ribu. The scale words are spoken in order from the largest group to the smallest, the same way the digits are written."
    - q: "What about the number one?"
      a: "In front of a scale word it shortens: 1000 is seribu and 100 is seratus, not satu ribu and satu ratus. Everywhere else it stays satu, as in satu juta."
    - q: "How large a number can it handle?"
      a: "Up to sixteen digits, which covers kuadriliun. Past that the exact value can no longer be held as a number in the page, so the tool asks for a smaller one instead of printing something wrong."

discount-calculator:
  about: >-
    Four everyday discount questions in one place: the final price after a
    discount, the discount percent between two prices, the original price when
    you only know what you paid, and several discounts stacking one after
    another. Switch the mode and the fields relabel themselves, then the result
    shows the price, the discount and the amount saved.
  faq:
    - q: "How do I find the discount I was given?"
      a: "Pick the second mode, enter the full price and the price you actually paid, and the page divides the difference by the full price. That is the percentage off, not the percentage of the original that you paid."
    - q: "Why does it need a price and a final price for the reverse?"
      a: "A percentage on its own is not enough. Two prices give the ratio between them, which is all the discount percent is; with only one price there is nothing to compare against."
    - q: "Can the discount be over 100%?"
      a: "The final price can fall to zero but not below it, so a discount of 100% or more has no original price. The page says so rather than returning a negative result."
    - q: "How does the stacked mode work?"
      a: "Enter one discount per line and the page applies them in order, each on the price the previous one left behind. Two 20% discounts are 36% off together, because the second 20% comes off the reduced price, not the original."

unit-price-comparator:
  about: >-
    Compare pack sizes on a level footing. List each product as name, price,
    quantity and unit, and the page ranks them by price per unit, marking the
    cheapest in each group. Millilitres and litres, or grams and kilograms, are
    normalised first, so a 600 ml bottle and a 1.5 litre bottle can be judged
    against each other.
  faq:
    - q: "Why does it group by unit?"
      a: "Price per millilitre and price per gram are not comparable, so each base unit gets its own ranking. Everything with a volume unit lands in one group, everything with a mass unit in another."
    - q: "What units are recognised?"
      a: "ml, l, g, kg, m and cm, plus a few spelled-out forms, and pcs and buah for counted items. Write price and quantity as plain numbers, without thousand separators, and separate the four fields with commas."
    - q: "Is the cheapest always the best buy?"
      a: "Only on price. Quality, shelf life and how much you will actually use are not in the numbers, so treat the ranking as one input rather than the answer."

age-calculator:
  about: >-
    Work out an age the way a birthday works: whole years, then whole months,
    then the leftover days. Alongside that it shows the totals in days, weeks,
    months and hours, the weekday someone was born on, and how long until the
    next birthday. Move the second date to see an age at any point.
  faq:
    - q: "Why not just divide the total days by 365?"
      a: "Because a year is not 365 days on average and months have different lengths. Counting whole years, then months, then days is how people read an age, and it stays correct across leap years."
    - q: "Which date is the second field for?"
      a: "It is the date you are measuring the age at. It defaults to today, but you can set it to a past date for a record or a future date to see how old someone will be."
    - q: "How is the next birthday counted?"
      a: "The page takes the birth month and day in the current year, or the next one if that has already passed this year, and counts the days between. A birthday today shows as today."

typo-spotter:
  about: >-
    A rule checker for the mistakes that spellcheck misses. It flags double
    spaces, a space before a comma or a full stop, a missing space after
    punctuation, repeated words such as the same word twice, runs of
    exclamation marks and spaces hugging brackets. Turn on the informal switch
    and it also lists common Indonesian short forms like yg, tdk and diatas with
    the fuller word to use instead.
  faq:
    - q: "Is this checking grammar?"
      a: "No. It only matches patterns, so it will not tell you whether a sentence makes sense and it does not know the difference between your and you're. It is a first pass for mechanical slips, not a replacement for reading the text."
    - q: "Why does it flag text that looks fine?"
      a: "Some rules are deliberately blunt. A missing-space rule will fire on e.g. and a repeated-word rule on a genuine repetition like had had, both of which are sometimes correct. Treat the list as suggestions and skim it."
    - q: "Does anything leave the page?"
      a: "No. The checking runs entirely in the browser, so drafts and internal notes can be pasted in without them being sent anywhere."

whatsapp-link-generator:
  about: >-
    Build a wa.me link that opens a chat with a number and a message already
    filled in. Enter the country code and the phone number, type the message,
    and the page writes the click-to-chat link, ready to paste into a page, an
    email or a QR code.
  faq:
    - q: "Why is the leading zero removed?"
      a: "wa.me needs the number in international form, which has no national trunk prefix. A local number written 0812 becomes 62812 once the country code is added, so the leading zero is dropped before the two are joined."
    - q: "Does the message have to be encoded?"
      a: "Spaces and punctuation cannot sit raw in a URL, so the message is percent-encoded. That is why a space turns into %20 in the link; WhatsApp decodes it back to a normal space when the chat opens."
    - q: "Can I shorten the link?"
      a: "Not here, because shortening needs a request to a third-party service and this page never sends your text anywhere. Use your own shortener afterwards if you need one."

whatsapp-formatter:
  about: >-
    Write a WhatsApp message without remembering which character makes what
    bold. Select some text and press a button to wrap it, or press a list button
    to prefix each line, then watch the preview update to show how the message
    will look. Bold, italic, strikethrough and monospace are all supported, and
    the raw marker characters stay in the text so you can copy it straight into
    the chat box.
  faq:
    - q: "Which markers does WhatsApp use?"
      a: "An asterisk on each side for bold, an underscore for italic, a tilde for strikethrough and three backticks for monospace. They only take effect when the character touches text with no space, which is why the wrap button adds them snugly."
    - q: "Do the buttons need a selection?"
      a: "No. With nothing selected the markers are inserted as a pair and the caret is placed between them, so you can type the text afterwards. With a selection the markers go around it."
    - q: "Can the preview differ from WhatsApp?"
      a: "Slightly. The page renders the markers as HTML for a quick look, while WhatsApp applies its own rules, so an unusual nesting of markers may look a little different in the app. The copy button always gives you the plain text with markers."

csv-viewer:
  about: >-
    Drop in a CSV, TSV or pipe-separated file and read it as a table. The
    delimiter is guessed from the first line, but you can sort by any column,
    type in the filter box to narrow the rows, and see the shape of the file at
    a glance. The file is parsed in the page, so nothing is uploaded.
  faq:
    - q: "How does it pick the delimiter?"
      a: "It counts commas, semicolons, tabs and pipes on the first non-empty line and uses whichever appears most. A file with a single column falls back to the comma."
    - q: "How large a file can it open?"
      a: "The whole file is parsed, but only the first five hundred rows are drawn to keep the page responsive. The row count in the corner always reflects the full file, not just what is shown."
    - q: "Does it read quoted fields?"
      a: "Yes. A field wrapped in double quotes may contain the delimiter, a newline or a doubled quote, and the viewer keeps it as one value rather than splitting it."

csv-json-converter:
  about: >-
    Move between the two shapes that CSV and JSON use for tabular data. Going
    to JSON, each row becomes an object keyed by the header; going to CSV, the
    keys become the header and any key a row is missing is left empty. The
    delimiter is detected for CSV input and can be set by hand.
  faq:
    - q: "What does the JSON have to look like?"
      a: "An array. Each item can be an object, whose keys become columns, or an array, which is written as a plain row. Anything that is not an array is rejected with a message rather than a partial result."
    - q: "Where does the extra header come from?"
      a: "When objects have different keys, the header is the union of all of them, in the order the keys first appear. A row that lacks one of those keys simply gets an empty cell."
    - q: "Are values converted to numbers?"
      a: "No. CSV cells are text, so 120 in a CSV file becomes the string \"120\" in JSON. Convert the types in your own code if the number matters."

cli-table-converter:
  about: >-
    Turn the columns printed by docker ps, ps, df or kubectl into a table you
    can paste into a document or a ticket. The separator is detected from the
    text, and the output can be a Markdown table, CSV or TSV. Rows of different
    lengths are padded so the result is always rectangular.
  faq:
    - q: "Why does the automatic mode choose two spaces?"
      a: "In aligned terminal output a single space can appear inside a value, while two or more spaces are the gap between columns. Splitting on two spaces keeps names with one space intact, and any-run-of-spaces is available when a column is narrow."
    - q: "What happens to a pipe in the data?"
      a: "In a Markdown table a pipe would end the cell, so it is escaped as a backslash followed by the pipe. In CSV and TSV output the usual quoting rules apply instead."
    - q: "Can I drop the header?"
      a: "Clear the first-row box and the table is written without one. In Markdown output that means generic column names are used, because the format expects a header line."

maps-link-parser:
  about: >-
    Paste a Google Maps link and the page pulls out the latitude and longitude,
    then writes them the way you need them: a plain pair, degrees minutes and
    seconds, JSON, CSV, a SQL INSERT or a GeoJSON point. A label you type is
    carried into the formats that have a place for it.
  faq:
    - q: "Why does a short link not work?"
      a: "A maps.app.goo.gl link only reveals its coordinates after a network request follows the redirect, and this page makes no requests. Open the link once in a browser and paste the full URL from the address bar."
    - q: "Which part of the link is used?"
      a: "The viewport centre after the @ sign comes first, then the place coordinates marked with !3d and !4d, then query parameters such as q or destination. The first valid pair wins."
    - q: "Why are the coordinates rounded?"
      a: "Six decimal places is about ten centimetres on the ground, which is finer than any map pin. Rounding there keeps the numbers readable without losing meaningful precision."

exif-remover:
  about: >-
    A photo carries more than the picture: the camera model, the time it was
    taken, the software that saved it and often the exact place on the map.
    Re-encoding the pixels through a canvas rebuilds the image without any of
    that, so what you share carries only what you can see.
  faq:
    - q: "Does re-encoding change how the picture looks?"
      a: "It redraws the same pixels, so it looks the same to the eye. A JPEG is saved again at the chosen quality, which costs a little sharpness; choosing PNG or a high quality keeps that to a minimum."
    - q: "Why does the file sometimes get bigger?"
      a: "The original may have been compressed very hard or stored as a smaller size. Re-encoding at a high quality can produce more bytes than you started with, and the change line shows that rather than hiding it."
    - q: "Is the metadata really gone?"
      a: "Yes. The output is written fresh from the canvas, and a canvas only holds pixels, so EXIF, GPS, XMP and any embedded thumbnail have no way to come along."

image-format-converter:
  about: >-
    Save a picture in another format so it fits where it is going. JPEG keeps
    photographs small, PNG keeps sharp edges and transparency, WebP often beats
    both, and AVIF is offered when the browser can actually write it.
  faq:
    - q: "Why is AVIF marked as not supported?"
      a: "Encoding AVIF is left to the browser, and some browsers can decode it without being able to write it. The page encodes one pixel and checks what format comes back before offering the choice."
    - q: "What happens to transparency in a JPEG?"
      a: "JPEG has no alpha channel, so a transparent area has to become something. A white layer is laid down first, which is the usual choice for a photo going to a place that expects a solid background."
    - q: "Which format should I pick?"
      a: "Photographs are usually smallest as WebP or JPEG. Screenshots, diagrams and anything with text or a transparent background hold up better as PNG, and the size shown for each choice makes the trade visible."

watermark-tool:
  about: >-
    Put a name, a site or a date onto a picture so it travels with the image.
    You choose a corner, a size as a share of the shorter side, a colour and an
    opacity, and the text is drawn into the pixels when you download.
  faq:
    - q: "How large should the watermark be?"
      a: "Four per cent of the shorter side is small enough to stay out of the way but large enough to read on a phone. Raise it toward ten per cent when the picture will be shown at a large size."
    - q: "Why is there a dark outline?"
      a: "White text disappears on a bright sky and black text disappears in shadow. A thin dark stroke around the letters keeps them readable on either, and it can be turned off when the background is known."
    - q: "Can someone remove the watermark?"
      a: "The text is part of the pixels rather than a separate layer, so removing it means editing the image. A determined person still can, which is why a watermark deters rather than prevents."

photo-target-resizer:
  about: >-
    Some forms and services cap an upload at five hundred kilobytes or a
    megabyte. Give this tool the limit and the picture is scaled to fit, then
    re-encoded at the highest quality that stays under it, with the result
    shown before you download.
  faq:
    - q: "How is the right quality found?"
      a: "The encoder is tried at a series of qualities from high to low and stops at the first result that fits. That gives the best-looking file the limit allows rather than a guess."
    - q: "What if the limit is too small?"
      a: "The smallest the encoder can produce is reported, along with the quality it used. Lower the longest edge, which removes pixels rather than quality, and try again."
    - q: "Which format comes out?"
      a: "JPEG by default, because it is the most widely accepted. With the WebP box ticked the same picture is also encoded as WebP, and whichever of the two is smaller is the one you get."

screenshot-privacy-cleaner:
  about: >-
    Screenshots are easy to share and easy to over-share. Drag a box over an
    email address, a phone number, an IP address or a line of text, and either
    pixelate it or cover it with a solid black rectangle, then download the
    cleaned-up copy.
  faq:
    - q: "Does it find the sensitive parts for me?"
      a: "No. Locating a phone number inside a picture would need optical character recognition, which would mean shipping a large model and reading every pixel of your screenshot. Marking the areas by hand is the honest version."
    - q: "Is pixelating as safe as a black box?"
      a: "A black box destroys the pixels underneath. Pixelation keeps an average of them, and a coarse enough block makes them unreadable, but for anything truly secret the black box is the safer choice."
    - q: "Can I take a redaction back?"
      a: "Undo removes the last box and Reset clears them all, because the untouched pixels are kept aside while you work. Once you download, the redactions are baked into the saved file."

zip-builder:
  about: >-
    Pick a handful of files and get one zip back, without installing anything.
    The archive is assembled in the page from the bytes already on your disk,
    so nothing is uploaded, and a compression level lets you trade a little
    time for a smaller download.
  faq:
    - q: "Why do two files with the same name both survive?"
      a: "A zip cannot hold two entries under one name, so the second becomes name-1, then name-2 and so on. That keeps both files instead of quietly losing one."
    - q: "Which compression level should I use?"
      a: "The balanced level suits almost everything. Store only is quicker and is the right choice for files that are already compressed, such as a JPEG or another zip, where no level can help."
    - q: "Is there a size limit?"
      a: "Only what your device's memory allows, since the archive is built in the page. A few hundred megabytes of photographs is fine on a desktop and heavy on a phone."

pdf-info:
  about: >-
    Open a PDF to see what it is made of: how many pages there are, the exact
    size of each one, the format version and the document information the
    producer wrote, such as the title and the author.
  faq:
    - q: "What are the point and millimetre figures?"
      a: "A PDF measures its pages in points, where seventy-two points make an inch. Both units are shown because print shops usually ask in millimetres while the file stores points."
    - q: "Why is some document information missing?"
      a: "The title, author and subject are optional fields. Plenty of producers leave them empty or fill in only a producer name, and this page shows what is actually there rather than guessing."
    - q: "Can it read an encrypted PDF?"
      a: "A password-protected PDF cannot be parsed without the password, so the page says so instead of showing an empty report. Removing the password with the owner's permission has to happen in a reader that can open it."

pdf-page-extractor:
  about: >-
    Pull the pages you want out of a long PDF, or cut the pages you do not
    want by listing only the ones to keep. The chosen pages are copied into a
    new document, so the parts you left out are genuinely absent from the
    result rather than merely hidden.
  faq:
    - q: "How do I write a page range?"
      a: "Separate the parts with commas: 1-3, 7, 10- means the first three pages, page seven and everything from page ten onward. A leading dash as in -4 means from the first page to the fourth."
    - q: "Does the original file change?"
      a: "No. The file on your disk is only read. The result is a separate download, and the original keeps all of its pages."
    - q: "Do links and form fields survive?"
      a: "The page content comes across, but annotations such as form fields and internal links are not carried over, because only the pages themselves are copied into the new document."

pdf-merger:
  about: >-
    Combine several PDFs into a single document. The files are listed in the
    order they will appear, and the order can be changed before the result is
    built, which matters when a cover page or an appendix has to land in the
    right place.
  faq:
    - q: "Can I merge PDFs with different page sizes?"
      a: "Yes. Each page keeps its own size, so a mixed document will have pages of different dimensions. Readers and printers handle that; a print shop may prefer a consistent size."
    - q: "What happens if one file cannot be read?"
      a: "That file is named in the status and skipped, while the rest are merged. An encrypted PDF is the usual reason, since it cannot be parsed without its password."
    - q: "Is the order the order I selected them in?"
      a: "Files are taken in the order the picker returned them, which is usually the order shown in your file browser. Use the Up and Down buttons to set the order you actually want."

text-to-pdf:
  about: >-
    Turn a note, a receipt or a block of plain text into a PDF. The text is
    wrapped to the page width and carried onto a new page when the space runs
    out, with a choice of page size, font, size, margin and line spacing.
  faq:
    - q: "Which characters can be drawn?"
      a: "The standard PDF fonts cover Latin-1, which is enough for English and Indonesian and most Western European text. Characters outside that range, such as emoji or Cyrillic, cannot be drawn and are left out, and the count is reported."
    - q: "How does the wrapping work?"
      a: "Each word is measured with the chosen font and size, and a line breaks when the next word would cross the margin. That keeps the layout correct even in a proportionally spaced font like Helvetica or Times."
    - q: "Can I use my own font?"
      a: "Not in this version. Embedding a font means shipping the font file, which would add weight to the page. The three standard fonts are always available and need nothing extra."

mic-tester:
  about: >-
    Find out whether a microphone is picking anything up, and how loudly. The
    browser opens the input and the level is measured on a bar with a peak
    marker, so a quiet headset or a muted laptop mic is obvious at a glance.
  faq:
    - q: "Is anything recorded or sent anywhere?"
      a: "No. The audio is read through an analyser inside the page and turned into a number. There is no recorder attached, no buffer is kept, and no request leaves the browser."
    - q: "Why does the browser ask for permission?"
      a: "Reading a microphone requires an explicit grant by design, and it is asked for the first time you press Start listening. If it was blocked earlier, the padlock in the address bar is where you allow it again."
    - q: "Why does the bar barely move when I speak?"
      a: "Usually the wrong input device is selected, or its gain is low in the system sound settings. Also check that automatic gain control is not fighting the test, and try again closer to the microphone."

gamepad-tester:
  about: >-
    Inspect a connected controller: every axis with its travel, every button
    with its pressure, and a running note of the last input seen. It is the
    quick answer to whether a pad works, which stick drifts and which button
    has stopped registering.
  faq:
    - q: "Why is the controller missing until I press a button?"
      a: "Browsers hide gamepads until an input event proves one is being used, which is a privacy measure. Press any button on the pad and it will appear, then stay listed until it is disconnected."
    - q: "A stick reports a small value while untouched. Is it broken?"
      a: "A little drift below about 0.05 is normal on worn analogue sticks and browsable games compensate with a dead zone. A value that keeps climbing on its own, or a stick that never reaches 1 at full travel, points to a real fault."
    - q: "Can it test vibration or the light bar?"
      a: "Not here. Those need the haptics actuators a browser exposes separately, and support varies by pad and by browser, so this page sticks to the axes and buttons that every controller reports."

openapi-viewer:
  about: >-
    Paste an OpenAPI or Swagger document and read it as a list of operations
    rather than as a wall of YAML. Each endpoint shows its method, its
    parameters, its request body and the responses it can return, with
    references resolved so a schema appears where it is used.
  faq:
    - q: "Does it fetch my specification from a URL?"
      a: "No. The document is read as text in the page, and a reference to another file is shown as a reference rather than followed. That keeps the page working offline and keeps a private spec private."
    - q: "Which versions are supported?"
      a: "OpenAPI 3.0 and 3.1 as well as Swagger 2.0. The overview writes down which one it found, and a Swagger 2 document's host, basePath and schemes are shown as the server."
    - q: "How deep does a schema go?"
      a: "Nested objects are expanded a couple of levels and then summarised as object, so one schema cannot flood the page. The shape is still clear, and the full definition is in the document you pasted."

background-remover:
  about: >-
    Make the background of a photo transparent. Pixels along the edge are
    flooded inwards and everything close enough in colour goes, which turns a
    product shot on a plain surface or a screenshot on a flat colour into a
    cut-out PNG in one step.
  faq:
    - q: "Will this work on a photo with a busy background?"
      a: "No, and no colour match can. A street, a room or a crowd needs a segmentation model that understands what a person or an object is. This page is honest about that limit rather than producing a ragged edge that has to be cleaned up by hand."
    - q: "Why is part of the subject disappearing?"
      a: "The subject shares a colour with the background and the match is too generous. Lower the colour match until it stops, or start from the four corners when only the edges are reliable."
    - q: "What happens to the parts that are left?"
      a: "They keep their colour and position exactly, and the cleared area becomes fully transparent. The download is a PNG or a WebP because JPEG cannot store transparency at all."
